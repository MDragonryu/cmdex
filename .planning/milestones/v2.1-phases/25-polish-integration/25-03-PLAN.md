---
phase: 25-polish-integration
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - terminal_service.go
  - terminal_service_test.go
  - frontend/src/locales/en.json
  - frontend/src/App.tsx
  - AGENTS.md
  - CHECKPOINT.md (NEW at .planning/phases/25-polish-integration/CHECKPOINT.md)
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "`CreateSession` returns an error of the form `CreateSession: max sessions reached (10)` when the active session count would reach `MaxSessions` (D not yet cited — see decision D-13 in the closure summary; new functional constant + guard)"
    - "The frontend `createTerminalSession` callback in `App.tsx` catches the new error and surfaces a localized `toast.maxSessionsReached` toast"
    - "The existing last-session close guard in `App.tsx:closeTerminalSession` (line 209) and `TerminalTabBar.tsx` (`isLastTab` prop, line 131) is confirmed in place (no change)"
    - "The existing PTY start failure toast in `Terminal.tsx:292` (`toast.error('Terminal start failed')`) is confirmed in place (no change)"
    - "`GOOS=windows go build ./...` exits 0 from darwin — the conpty stub compiles (D-13 cross-compile verification)"
    - "`AGENTS.md` contains a one-line note pointing to `CHECKPOINT.md` for the full Windows conpty verification gap (D-14)"
    - "`CHECKPOINT.md` contains a `## Windows conpty verification` section documenting the mock scope, cross-compile coverage, and the real-Windows gap (D-13, D-14)"
    - "The new `TestTerminalService_MaxSessionsLimit` test uses `newTestTerminalServiceWithMock` so the loop does not panic on a nil `ptyBackend`; the test asserts the limit is enforced on the 11th call regardless of backend type"
  artifacts:
    - path: "terminal_service.go"
      provides: "const MaxSessions = 10 + len(s.sessions) >= MaxSessions guard at the start of CreateSession"
      contains: "MaxSessions = 10"
    - path: "terminal_service_test.go"
      provides: "TestTerminalService_MaxSessionsLimit"
      contains: "TestTerminalService_MaxSessionsLimit"
    - path: "frontend/src/locales/en.json"
      provides: "toast.maxSessionsReached translation key"
      contains: "maxSessionsReached"
    - path: "frontend/src/App.tsx"
      provides: "createTerminalSession catch block detects 'max sessions reached' and surfaces the new toast"
      contains: "max sessions reached"
    - path: "AGENTS.md"
      provides: "One-line Windows conpty verification gap note in the Tests section"
      contains: "Windows conpty verification gap"
    - path: "CHECKPOINT.md"
      provides: "Phase 25 checkpoint with a Windows conpty verification section"
      contains: "## Windows conpty verification"
  key_links:
    - from: "terminal_service.go:CreateSession"
      to: "MaxSessions constant"
      via: "len(s.sessions) check at the start of the function"
      pattern: "len\\(s\\.sessions\\) >= MaxSessions"
    - from: "App.tsx:createTerminalSession"
      to: "toast.maxSessionsReached"
      via: "catch block string match on 'max sessions reached'"
      pattern: "max sessions reached"
    - from: "CHECKPOINT.md"
      to: "pty_backend_windows.go"
      via: "documentation reference to the conpty stub"
      pattern: "pty_backend_windows"
---

<objective>
Add the missing max-sessions error guard and surface it to the frontend. Verify the existing last-session close guard and PTY start failure toast are in place (no edits needed — these are already implemented in prior phases). Cross-compile to Windows from darwin to catch conpty API mismatches at compile time (D-13). Document the Windows conpty verification gap in CHECKPOINT.md and add a one-line note to AGENTS.md so future maintainers know the gap exists (D-14).

Citations: D-13 (cross-compile verification), D-14 (CHECKPOINT.md + AGENTS.md).
</objective>

<execution_context>
@/Users/mac/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/mac/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-polish-integration/25-CONTEXT.md
@terminal_service.go
@terminal_service_test.go
@pty_backend_windows.go
@.planning/phases/25-polish-integration/25-01-PLAN.md
@.planning/phases/25-polish-integration/25-02-PLAN.md
@frontend/src/App.tsx
@frontend/src/locales/en.json
@frontend/src/components/TerminalTabBar.tsx
@frontend/src/components/Terminal.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MaxSessions constant + guard in CreateSession + backend test + frontend toast</name>
  <files>terminal_service.go, terminal_service_test.go, frontend/src/locales/en.json, frontend/src/App.tsx</files>
  <read_first>
    - terminal_service.go `CreateSession` (lines 155-199) — the place to add the guard; the function holds `s.mu.Lock()` at the top
    - terminal_service.go `SessionInfo` struct (lines 24-30) and the `sessionState` struct (lines 33-55) — to confirm the `workingDir` field shape is unchanged
    - terminal_service_test.go `TestTerminalService_CreateSession` (line 243) — the pattern for tests that call `s.ServiceShutdown()` as cleanup
    - frontend/src/App.tsx `createTerminalSession` (lines 193-206) — the catch block to extend
    - frontend/src/locales/en.json `toast` section (lines 182-194) — where to add the new key
    - frontend/src/components/TerminalTabBar.tsx lines 131 (hides X for last tab) and 151 (disables context menu) — confirm the existing last-tab guard
    - frontend/src/components/Terminal.tsx line 292 — confirm the existing PTY start failure toast
  </read_first>
  <action>
    Step 1: Add a package-level constant to `terminal_service.go`. Place it directly above the `SessionInfo` struct (around line 23) with a doc comment that reads `// MaxSessions is the maximum number of concurrent terminal sessions. Beyond this limit, CreateSession returns an error to protect against unbounded resource use.` and the value `const MaxSessions = 10`. The number 10 is chosen as a reasonable cap that exceeds normal user workflows (typical use is 1-3 sessions) while preventing accidental resource exhaustion.

    Step 2: In `CreateSession` (line 156), insert the guard immediately after the `s.mu.Lock()` call and before the `s.sessionCounter++` line. The guard must be:
       - Read `n := len(s.sessions)` while holding the lock.
       - If `n >= MaxSessions`, unlock and return `nil, fmt.Errorf("CreateSession: max sessions reached (%d)", MaxSessions)`.
    Do NOT add a `t.Cleanup` for sessions inside this branch — the failure path returns before any session is created. The `delete(s.sessions, id)` path at line 186 (used when `startSessionLocked` fails AFTER a session was created) is unchanged.

    Step 3: Add `TestTerminalService_MaxSessionsLimit` to `terminal_service_test.go`. The test must:
    - Skip when `runtime.GOOS != "darwin"` (the mock is darwin-only per Plan 25-01).
    - Skip when `testing.Short()` is true.
    - Construct a `TerminalService` with `newTestTerminalServiceWithMock(t)` (created in Plan 25-01 Task 2) and `defer s.ServiceShutdown()`. The mock backend must be used so the test does not depend on a real PTY. The `newTestTerminalService` helper in Plan 25-01's Step 6 does inject `s.ptyBackend = newPtyBackend()`, so technically the test could use it too — but `newTestTerminalServiceWithMock` is the explicit, no-process-spawning choice and is the correct one for a max-sessions guard test (the test is about session count, not PTY behavior).
    - Loop `MaxSessions` iterations calling `s.CreateSession()` and assert each returns no error.
    - Call `s.CreateSession()` one more time (the `MaxSessions+1`-th). Assert the returned error is non-nil and its `Error()` string contains the substring `max sessions reached` (verified with `strings.Contains`).
    - Cleanup is automatic via `s.ServiceShutdown()` — the mock's `sleep 0.05` process per session is short-lived; `ServiceShutdown` will close the remaining sessions.

    Step 4: Add a toast translation key. In `frontend/src/locales/en.json`, locate the `toast` object (around line 182) and add a new key `maxSessionsReached` with the value `Maximum number of terminal sessions ({{limit}}) reached. Close a session to create a new one.` Place it adjacent to the other `toast.*` keys, preserving the JSON formatting (commas, indentation matching the surrounding two-space indent).

    Step 5: Update `createTerminalSession` in `frontend/src/App.tsx` (lines 193-206). Replace the `catch (err) { ... toast.error('Could not create session. Check that the terminal backend is running.'); }` block with logic that inspects the error string. Concretely:
    - Capture the error message into a `const msg = String(err)`.
    - If `msg.includes('max sessions reached')`, call `toast.error(t('toast.maxSessionsReached', { limit: 10 }))`.
    - Otherwise, fall back to the existing generic message `toast.error('Could not create session. Check that the terminal backend is running.')`.
    - Keep the existing `console.error('Failed to create terminal session:', err)` log line above the toast call.

    Step 6: Verify the existing error states are in place (no edits; document in SUMMARY). The verification points:
    - Last-session close guard — `App.tsx:closeTerminalSession` line 209 has `if (sessions.length <= 1) return;` (comment: `// D-02: last tab not closeable`).
    - Last-tab UI hiding — `TerminalTabBar.tsx` line 131 wraps the close `<span>` in `{!isLastTab && (...)}`; line 151 has `disabled={isLastTab}` on the context menu item.
    - PTY start failure toast — `Terminal.tsx` line 292 calls `toast.error('Terminal start failed')` inside the `Start(sessionId, current.cols, current.rows).catch(...)` block.
    - Session create failure toast — `App.tsx:createTerminalSession` line 204 (the line we are updating) already toasts on generic error; the new branch handles the max-sessions case specifically.
    Confirm each of these is present by reading the files; do not edit them.

    Step 7: Run `go test -run "TestTerminalService_MaxSessionsLimit" -v ./...` and `cd frontend && pnpm tsc --noEmit` to confirm the new code compiles and the test passes.
  </action>
  <acceptance_criteria>
    - `terminal_service.go` contains `const MaxSessions = 10` (verified by grep)
    - `terminal_service.go` `CreateSession` contains a `len(s.sessions) >= MaxSessions` check before the `s.sessionCounter++` line (verified by grep)
    - The error returned by the new branch contains the substring `max sessions reached` (verified by grep on the `fmt.Errorf` call)
    - `terminal_service_test.go` contains `TestTerminalService_MaxSessionsLimit` (verified by grep)
    - `TestTerminalService_MaxSessionsLimit` uses `newTestTerminalServiceWithMock` (verified by grep) — required so the loop does not panic on a nil `ptyBackend` after Plan 25-01's refactor
    - `go test -run TestTerminalService_MaxSessionsLimit -v ./...` passes
    - `frontend/src/locales/en.json` contains a key `maxSessionsReached` under the `toast` object (verified by grep)
    - `frontend/src/locales/en.json` `maxSessionsReached` value references the interpolation `{{limit}}`
    - `frontend/src/App.tsx` `createTerminalSession` catch block contains the string `max sessions reached` (verified by grep)
    - `frontend/src/App.tsx` `createTerminalSession` catch block calls `t('toast.maxSessionsReached'` (verified by grep)
    - `pnpm tsc --noEmit` in `frontend/` reports zero errors
    - `App.tsx:closeTerminalSession` still contains `if (sessions.length <= 1) return;` (existing last-tab guard preserved)
    - `TerminalTabBar.tsx` still contains `!isLastTab` (existing UI guard preserved)
    - `Terminal.tsx` still contains `toast.error('Terminal start failed')` (existing PTY start failure toast preserved)
  </acceptance_criteria>
  <verify>
    <automated>go test -run TestTerminalService_MaxSessionsLimit -v ./... && (cd frontend && pnpm tsc --noEmit)</automated>
  </verify>
  <done>
    A `const MaxSessions = 10` package-level constant exists. `CreateSession` returns a `max sessions reached` error when the limit would be exceeded. The new test asserts the guard fires on the 11th call. The frontend surfaces the new error with a localized toast, and falls back to the existing generic message for other errors. The existing last-tab close guard, PTY start failure toast, and session create failure toast are all confirmed in place without modification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-compile verification (D-13) + CHECKPOINT.md + AGENTS.md update (D-14)</name>
  <files>CHECKPOINT.md (NEW at .planning/phases/25-polish-integration/CHECKPOINT.md), AGENTS.md</files>
  <read_first>
    - pty_backend_windows.go (the conpty stub created in Plan 25-01; this is what cross-compile must accept)
    - pty_backend_unix.go (the darwin/creack/pty impl; reference for the build-tag pattern)
    - .planning/phases/25-polish-integration/25-CONTEXT.md (the four CONTEXT.md areas: persistence scope, cwd, leak verification, conpty)
    - AGENTS.md (the file to update; specifically the `## Tests` section near the bottom, where the existing `db_test.go` line lives)
  </read_first>
  <action>
    Step 1: Run the cross-compile verification. From the repo root, run `GOOS=windows go build ./...` and capture the exit code. The command MUST exit 0. If it fails, read the error output, fix the offending file (most likely `pty_backend_windows.go` or a transitive include), re-run, and record the original error in the SUMMARY. The cross-compile catches conpty API mismatches at compile time — it does not run any Windows code, so it cannot catch runtime issues.

    Step 2: Create `.planning/phases/25-polish-integration/CHECKPOINT.md`. The file must contain, in order:
    - Top-level heading `# Phase 25 Checkpoint`
    - Metadata line: `**Phase:** 25-polish-integration` and `**Date:** 2026-06-16` and `**Status:** Complete`
    - Section `## Summary` listing the seven phase deliverables from CONTEXT.md (stress test, ptyBackend interface, cross-compile, cwd inheritance, dead-code cleanup, error states, Windows docs)
    - Section `## Windows conpty verification` containing these subsections:
      - `### Scope of verification in this phase` — explicitly list: `GOOS=windows go build ./...` from darwin (compile-time); the 100-cycle stress test running against the `mockPtyBackend` (orchestration); and explicitly state the conpty stub in `pty_backend_windows.go` returns `Windows PTY support not yet implemented` errors and is a placeholder.
      - `### Out of scope (acknowledged gap)` — explicitly list: runtime conpty verification (no Windows machine in this milestone); process group / signal handling on Windows; shell detection on Windows via the `detectShell` Windows branch (lines 109-114 of `terminal_service.go`).
      - `### Future work` — list four items: add `github.com/UserExistsError/conpty` to `go.mod`; replace `conptyBackend` stub with a real implementation; add a `windows-latest` CI runner to exercise the full lifecycle; add a Windows-tagged variant of `TestTerminalService_CreateSession`.
    - Section `## Files` listing the new, modified, and removed files from all four plans of this phase.
    - Section `## Test coverage` listing the four new test functions (from Plans 25-02 and 25-03) plus a one-line note that the existing twelve multi-session tests still pass.
    - Section `## Decisions honored` listing all 14 locked decisions D-01..D-14 with one-line summaries (D-01: Phase 22 skipped; D-02: ROADMAP updated; D-03: PERS-01..PERS-04 moved to v2; D-04..D-07: cwd inheritance; D-08: stress test; D-09: RUNBOOK; D-10: no new test framework; D-11: ptyBackend; D-12: darwin mock; D-13: cross-compile; D-14: docs).

    Step 3: Update `AGENTS.md`. The current file has a `## Tests` section (around line 130) with the line `- Go: db_test.go — three migration tests...`. Add a new bullet directly below the `db_test.go` line, in the same list format. The new bullet must read: `- **Windows conpty verification gap:** The conpty backend in `pty_backend_windows.go` is a stub that returns "not implemented" errors. Runtime conpty testing is not done in this milestone — see `.planning/phases/25-polish-integration/CHECKPOINT.md` for the full gap documentation.` Do not restructure the existing list; just append the bullet.

    Step 4: Verify the cross-compile + docs end-to-end. Re-run `GOOS=windows go build ./...` to confirm the exit code is still 0 after the docs edits (the docs edits do not affect the build, but a final run is cheap insurance). Then run `grep -c "## Windows conpty verification" .planning/phases/25-polish-integration/CHECKPOINT.md` and `grep -c "Windows conpty verification gap" AGENTS.md` to confirm both new strings are present and the count is exactly 1.
  </action>
  <acceptance_criteria>
    - `GOOS=windows go build ./...` from darwin exits 0
    - `GOOS=linux go build ./...` from darwin exits 0 (sanity — the unix build is the default and should also still work)
    - The file `.planning/phases/25-polish-integration/CHECKPOINT.md` exists
    - `CHECKPOINT.md` contains the heading `# Phase 25 Checkpoint`
    - `CHECKPOINT.md` contains the section `## Windows conpty verification`
    - `CHECKPOINT.md` `## Windows conpty verification` section contains a `### Scope of verification in this phase` subsection
    - `CHECKPOINT.md` `## Windows conpty verification` section contains a `### Out of scope (acknowledged gap)` subsection
    - `CHECKPOINT.md` `## Windows conpty verification` section contains a `### Future work` subsection
    - `CHECKPOINT.md` mentions `pty_backend_windows.go` and `mockPtyBackend` (verified by grep)
    - `CHECKPOINT.md` contains a `## Decisions honored` section listing D-01 through D-14
    - `AGENTS.md` contains the phrase `Windows conpty verification gap` (verified by grep)
    - `AGENTS.md` references `.planning/phases/25-polish-integration/CHECKPOINT.md` (verified by grep)
    - `go vet ./...` reports no issues
  </acceptance_criteria>
  <verify>
    <automated>GOOS=windows go build ./... && grep -q "## Windows conpty verification" .planning/phases/25-polish-integration/CHECKPOINT.md && grep -q "Windows conpty verification gap" AGENTS.md && echo "all checks passed"</automated>
  </verify>
  <done>
    Cross-compile to Windows from darwin succeeds with exit code 0. CHECKPOINT.md exists at the phase directory with a `## Windows conpty verification` section covering scope, out-of-scope gap, and future work, plus a `## Decisions honored` section listing D-01..D-14. AGENTS.md has a one-line bullet in the `## Tests` section that points to CHECKPOINT.md. Future maintainers know the Windows conpty gap exists and where to look.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MaxSessions guard | New code path; the limit prevents unbounded session creation but does not move any trust boundary. The constant is hard-coded; the error path returns to the caller (frontend) which surfaces a toast. |
| Cross-compile output | `GOOS=windows go build ./...` produces a Windows executable on the darwin host. The output is not executed in this plan; it is discarded. |
| CHECKPOINT.md / AGENTS.md | Documentation only. No code paths affected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Denial of Service (resource exhaustion) | `MaxSessions` constant in `terminal_service.go` | mitigate | The `len(s.sessions) >= MaxSessions` guard rejects calls beyond 10 sessions. The error path is reachable from the UI (toast); a user with too many sessions must close one before creating another. |
| T-25-03-02 | Information Disclosure | MaxSessions error string includes the integer `10` | accept | The integer is a public constant in the same package; it is not sensitive. The frontend uses it in the toast (`{{limit}}`) which is appropriate user-facing information. |
| T-25-03-03 | Tampering | Cross-compile artifact | accept | The Windows binary produced by `GOOS=windows go build ./...` is discarded; it is not signed, distributed, or executed. The build is solely a compile-time check. |
| T-25-03-SC | Tampering | Go module dependencies | mitigate | This plan adds no new Go module dependencies. The conpty stub uses only `os`, `os/exec`, and `strconv` from the standard library. No new direct require in `go.mod`. |
</threat_model>

<verification>
- `GOOS=windows go build ./...` exits 0 from darwin
- `go vet ./...` reports no issues
- The four new tests (`TestTerminalService_GlobalDefaultCwdInheritance`, `TestTerminalService_CwdInheritance_ExistingSessionUnaffected`, `TestTerminalService_StressCreateClose`, `TestTerminalService_MaxSessionsLimit`) all pass
- `CHECKPOINT.md` exists with the required sections
- `AGENTS.md` contains the new one-line note pointing to `CHECKPOINT.md`
- The existing twelve multi-session tests still pass
- The existing EXEC-03 chain tests still pass
</verification>

<success_criteria>
- `MaxSessions = 10` is enforced by `CreateSession` (D-13 partial; the new constant closes a known gap)
- The frontend surfaces a localized `toast.maxSessionsReached` message (with `{{limit}}` interpolation)
- The existing last-session close guard and PTY start failure toast are confirmed in place
- `GOOS=windows go build ./...` succeeds from darwin (D-13)
- `CHECKPOINT.md` documents the Windows conpty gap with scope, out-of-scope, and future-work subsections (D-14)
- `AGENTS.md` has a one-line note in the Tests section pointing to `CHECKPOINT.md` (D-14)
</success_criteria>

<output>
Create `.planning/phases/25-polish-integration/25-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**New constant (in `terminal_service.go`):**
- `const MaxSessions = 10` — package-level, placed above the `SessionInfo` struct

**New guard (in `terminal_service.go:CreateSession`):**
- `len(s.sessions) >= MaxSessions` check at the top of the function, returning `fmt.Errorf("CreateSession: max sessions reached (%d)", MaxSessions)`

**New test (in `terminal_service_test.go`):**
- `TestTerminalService_MaxSessionsLimit` — creates `MaxSessions` sessions, asserts the next call returns an error containing `max sessions reached`

**New i18n key (in `frontend/src/locales/en.json`):**
- `toast.maxSessionsReached` with value `Maximum number of terminal sessions ({{limit}}) reached. Close a session to create a new one.`

**Modified frontend code (in `frontend/src/App.tsx`):**
- `createTerminalSession` catch block — string-matches on `max sessions reached` and surfaces the localized toast, falling back to the existing generic message otherwise

**Modified doc (in `AGENTS.md`):**
- One new bullet in the `## Tests` section: `- **Windows conpty verification gap:** ... see .planning/phases/25-polish-integration/CHECKPOINT.md ...`

**New file:**
- `.planning/phases/25-polish-integration/CHECKPOINT.md` — phase checkpoint with `## Windows conpty verification` section covering scope, out-of-scope gap, and future work, plus a `## Decisions honored` section listing D-01..D-14
