---
phase: 24-session-aware-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - frontend/src/components/Terminal.tsx
  - frontend/src/App.tsx
  - frontend/src/components/OutputPane.tsx
  - frontend/src/wails/events.ts
  - frontend/src/style.css
  - frontend/src/locales/en.json
  - frontend/e2e/utils/selectors.ts
  - frontend/e2e/mocks/runtime.ts
  - frontend/bindings/cmdex/executionservice.js
  - frontend/bindings/cmdex/eventservice.js
  - frontend/bindings/cmdex/models.js
autonomous: true
requirements:
  - EXEC-01
  - EXEC-05
  - EXEC-06

must_haves:
  truths:
    - "Terminal.tsx no longer subscribes to the cmd-executing event (per D-04 / Pitfall 4)"
    - "TerminalComponent's `activeSessionId` prop is removed from its interface; App.tsx no longer passes `activeSessionId={activeSessionId}` to it on the TerminalComponent call site at line 1601 (the TerminalTabBar at line 1524 still receives its own `activeSessionId` prop — different component, different prop)"
    - "OutputPane.tsx is deleted (per D-04 / Open Question #2)"
    - "events.ts no longer references `cmdExecuting` in its const map or initEventNames()"
    - ".output-pane* CSS block is removed from style.css (the orphan output-pane / output-pane-header / output-pane-title / output-resize-handle / output-pane-body / output-cmd / output-copy-inline / output-cmd-prefix / output-empty-response / output-cursor / output-empty / output-collapse-btn / terminal-dots / terminal-dot rules and the @keyframes output-slide-in / output-slide-out / blink animations at lines 1796-1964)"
    - "commandDetail.runInTerminal, historyPane.*, and outputPane.* i18n keys are removed from en.json"
    - "e2e selectors.ts no longer exports `outputPane` or `historyPane`"
    - "e2e/mocks/runtime.ts no longer mocks RunInTerminal, GetExecutionHistory, ClearExecutionHistory, and the executionHistory array is no longer referenced"
    - "Regenerated Wails bindings drop `RunInTerminal`, `GetExecutionHistory`, `ClearExecutionHistory` from executionservice.js and `cmdExecuting` field from models.js EventNames class"
    - "`pnpm tsc --noEmit` and `go build ./...` both pass after the changes"
  artifacts:
    - path: "frontend/src/components/Terminal.tsx"
      provides: "Component with cmd-executing subscription removed, activeSessionId prop removed, unused eventNames import removed"
      contains: "term.onData"
    - path: "frontend/src/App.tsx"
      provides: "TerminalComponent call site without activeSessionId prop"
      contains: "sessionId={id}"
    - path: "frontend/src/components/OutputPane.tsx"
      provides: "DELETED — no longer exists"
    - path: "frontend/src/wails/events.ts"
      provides: "eventNames const without cmdExecuting, initEventNames() without cmdExecuting assignment"
      contains: "export const eventNames = {"
    - path: "frontend/src/style.css"
      provides: "CSS without .output-pane* rules"
      contains: ".terminal-pane"
    - path: "frontend/src/locales/en.json"
      provides: "en.json without commandDetail.runInTerminal, historyPane section, outputPane section"
      contains: "common.copyLastOutput"
    - path: "frontend/e2e/utils/selectors.ts"
      provides: "Selectors without outputPane or historyPane entries"
    - path: "frontend/e2e/mocks/runtime.ts"
      provides: "Runtime mock without RunInTerminal/GetExecutionHistory/ClearExecutionHistory handlers and without executionHistory state"
    - path: "frontend/bindings/cmdex/executionservice.js"
      provides: "Regenerated bindings without RunInTerminal, GetExecutionHistory, ClearExecutionHistory"
    - path: "frontend/bindings/cmdex/eventservice.js"
      provides: "Regenerated bindings (no code change expected beyond the models.js drop)"
    - path: "frontend/bindings/cmdex/models.js"
      provides: "Regenerated EventNames class without cmdExecuting field"
  key_links:
    - from: "frontend/src/components/Terminal.tsx (cleanup)"
      to: "frontend/src/wails/events.ts (eventNames)"
      via: "removal of cmdExecuting import path"
      pattern: "eventNames\\.cmdExecuting"
    - from: "frontend/bindings/cmdex/executionservice.js (regen)"
      to: "execution_service.go (Plan 01 result)"
      via: "wails3 generate bindings consumes the updated Go methods"
      pattern: "wails3 generate bindings"
    - from: "frontend/bindings/cmdex/models.js (regen)"
      to: "event_service.go (Plan 01 result)"
      via: "wails3 generate bindings regenerates the EventNames class without CmdExecuting field"
      pattern: "cmdExecuting"

---

## Artifacts This Phase Produces

| Symbol | Kind | File | Description |
|--------|------|------|-------------|
| `Terminal.tsx` (cleaned) | React component | `frontend/src/components/Terminal.tsx` | Drops `cleanupCmdExecuting` subscription (lines 334-345, 351), drops `activeSessionId` prop from `TerminalComponentProps` (line 16) and destructuring (line 28), drops `activeSessionIdRef` + its effect (lines 40-43), drops `eventNames` import (line 8) if no other reference remains |
| `TerminalComponent` props (cleaned) | TS interface | `frontend/src/components/Terminal.tsx` | Drops `activeSessionId: string` field — only `isVisible`, `theme`, `sessionId`, `onShellExit` remain |
| `<TerminalComponent>` call site (cleaned) | JSX | `frontend/src/App.tsx` (line 1601 area) | Removes `activeSessionId={activeSessionId}` from the props passed to TerminalComponent — TerminalTabBar at line 1524 still uses its own `activeSessionId` prop |
| `OutputPane.tsx` (DELETED) | File removed | `frontend/src/components/OutputPane.tsx` | 280-line orphan component, no remaining imports (per Pitfall 2 verification) |
| `eventNames` const (cleaned) | TS const | `frontend/src/wails/events.ts` | Drops `cmdExecuting: 'cmd-executing'` line 8 |
| `initEventNames()` (cleaned) | TS function | `frontend/src/wails/events.ts` | Drops `eventNames.cmdExecuting = names.cmdExecuting` line 19 |
| `.output-pane*` CSS rules (removed) | CSS | `frontend/src/style.css` | Removes ~170 lines (1796-1964) of dead CSS — the orphan .output-pane/.output-pane-header/.output-pane-title/.output-resize-handle/.output-pane-body/.output-cmd/.output-copy-inline/.output-cmd-prefix/.output-empty-response/.output-cursor/.output-empty/.output-collapse-btn/.terminal-dots/.terminal-dot rules and the @keyframes output-slide-in / output-slide-out / blink rules |
| `commandDetail.runInTerminal` i18n key (removed) | i18n | `frontend/src/locales/en.json` | Line 48 — no remaining consumers |
| `historyPane.*` i18n block (removed) | i18n | `frontend/src/locales/en.json` | Lines 167-174 — no remaining consumers |
| `outputPane.*` i18n block (removed) | i18n | `frontend/src/locales/en.json` | Lines 175-189 — no remaining consumers (OutputPane deleted in Task 1) |
| `outputPane` selector (removed) | TS const | `frontend/e2e/utils/selectors.ts` | Line 40 — no e2e test consumes it (grep verified) |
| `historyPane` selector (removed) | TS const | `frontend/e2e/utils/selectors.ts` | Line 41 — no e2e test consumes it (grep verified) |
| `RunInTerminal` mock (removed) | TS handler | `frontend/e2e/mocks/runtime.ts` | Line 339-340 — `1736747747` handler deleted |
| `GetExecutionHistory` mock (removed) | TS handler | `frontend/e2e/mocks/runtime.ts` | Line 343-344 — `2752844091` handler deleted |
| `ClearExecutionHistory` mock (removed) | TS handler | `frontend/e2e/mocks/runtime.ts` | Line 346-349 — `3022740230` handler deleted |
| `executionHistory` state (removed) | TS state | `frontend/e2e/mocks/runtime.ts` | Line 9 declaration + 3 reset references (RunCommand mock push at 335, ResetAllData at 387, __cmdexE2E.reset at 415) — D-09 compliance for the mock |
| `RunCommand` mock (cleaned) | TS handler | `frontend/e2e/mocks/runtime.ts` | Removes `executionHistory.push(record)` from the record-building block at lines 322-337 |
| `executionservice.js` (regen) | TS bindings | `frontend/bindings/cmdex/executionservice.js` | Wails regenerates this with 3 fewer exported functions (RunInTerminal, GetExecutionHistory, ClearExecutionHistory) |
| `eventservice.js` (regen) | TS bindings | `frontend/bindings/cmdex/eventservice.js` | Wails regenerates this; structural shape unchanged beyond models.js |
| `models.js` (regen) | TS bindings | `frontend/bindings/cmdex/models.js` | EventNames class drops the `cmdExecuting` field; ExecutionRecord / other model classes unchanged |

<objective>
Complete the Phase 24 deletion-heavy refactor on the frontend side: drop the `cmd-executing` subscription and `activeSessionId` prop from `Terminal.tsx`, remove the orphan `OutputPane.tsx`, clean up its i18n keys, CSS, e2e selectors, and e2e mocks, and regenerate the Wails bindings so they match the Go source changes from Plan 01. The terminal tab bar continues to use its own `activeSessionId` prop (separate from the one removed from `TerminalComponent`), and the terminal copy button at App.tsx:1572 keeps its `t('common.copyLastOutput')` i18n key (still in use).

Purpose: With Plan 01's Go changes landed, the frontend's `cmd-executing` subscription has no producer (the event is gone from the Go side). The `activeSessionId` prop on `TerminalComponent` was only consumed by that subscription's guard; with the subscription gone, the prop is dead. `OutputPane.tsx` is a 280-line orphan from the pre-Phase-23 era. The e2e mocks and selectors reference methods/components that are being removed. The Wails bindings need regeneration to drop the deleted Go methods and the dropped `CmdExecuting` field from the `EventNames` struct.

Output: Frontend code without any reference to the deleted event/methods/components. Regenerated `frontend/bindings/cmdex/{executionservice,eventservice,models}.js` files reflect the Go changes. `pnpm tsc --noEmit` and `go build ./...` both pass.
</objective>

<execution_context>
@/Users/mac/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/mac/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-session-aware-execution/24-CONTEXT.md (D-04, Canonical References, Integration Points)
@.planning/phases/24-session-aware-execution/24-RESEARCH.md (Pattern 3 event removal, Common Pitfalls 2/3/4, Runtime State Inventory)
@.planning/phases/24-session-aware-execution/24-PATTERNS.md (Terminal.tsx section, App.tsx section, OutputPane.tsx section, Shared Patterns E)
@.planning/phases/24-session-aware-execution/24-VALIDATION.md (Per-Task Verification Map rows 24-02-01..24-05-02)

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From frontend/src/components/Terminal.tsx (current state, before this plan):
- Line 8: `import { eventNames } from '../wails/events';` — used only by the cmd-executing subscription. After removing that subscription, grep for `eventNames` in Terminal.tsx to confirm there are zero other references. If zero, drop the import.
- Line 12-18: `interface TerminalComponentProps { isVisible; theme; sessionId; activeSessionId; onShellExit?; }` — drop `activeSessionId`.
- Line 27-28: `const TerminalComponent = forwardRef<TerminalHandle, TerminalComponentProps>(({ isVisible, theme, sessionId, activeSessionId, onShellExit }, ref) => {` — drop `activeSessionId` from destructuring.
- Lines 40-43: `const activeSessionIdRef = useRef(activeSessionId); useEffect(() => { activeSessionIdRef.current = activeSessionId; }, [activeSessionId]);` — delete both blocks.
- Lines 334-345: the `cleanupCmdExecuting = Events.On(eventNames.cmdExecuting, ...)` subscription — delete the entire block.
- Line 351: `cleanupCmdExecuting();` in the cleanup return — delete this line.
- KEPT unchanged:
  - Lines 246-254: `term.onData` → `Write(sessionIdRef.current, data)` — Ctrl+C keystroke path (EXEC-06, Phase 23's preserved behavior).
  - Lines 305-318: `pty-output:{sessionId}` subscription — output streaming (EXEC-05, Phase 23's preserved behavior).
  - Lines 320-328: `pty-exit:{sessionId}` subscription.
  - Lines 330-332: `pty-cleared:{sessionId}` subscription.

From frontend/src/App.tsx (current state, before this plan):
- Line 1522-1530: `<TerminalTabBar sessions={sessions} activeSessionId={activeSessionId} ... />` — KEPT (TerminalTabBar has its own `activeSessionId` prop at TerminalTabBar.tsx:29, 163, 174, 211). This is a different component from TerminalComponent.
- Line 1589-1612: `<TerminalComponent ... sessionId={id} activeSessionId={activeSessionId} onShellExit={...} />` — REMOVE the `activeSessionId={activeSessionId}` prop at line 1601. Keep `sessionId={id}` (line 1598), `isVisible` (line 1598), `theme` (line 1599), and `onShellExit` (line 1602).
- Line 1572: `{t('common.copyLastOutput')}` — KEPT. The i18n key is still in use for the terminal copy button. Only the `outputPane.*` and `historyPane.*` and `commandDetail.runInTerminal` i18n keys are removed.

From frontend/src/components/OutputPane.tsx (current state, before this plan):
- 280-line file at frontend/src/components/OutputPane.tsx. DELETE the entire file.
- Pre-delete verification: `grep -rn "from.*OutputPane" frontend/src/` must return zero matches. The RESEARCH.md initial scan confirms zero imports in `frontend/src/`. The OutputPane is also referenced by `frontend/e2e/utils/selectors.ts:40` (`outputPane: '[data-testid="output-pane"]'`) — that's a selector reference, not a TS import, and the selector is removed in Task 2.

From frontend/src/wails/events.ts (current state, before this plan):
- Line 8: `cmdExecuting: 'cmd-executing',` — REMOVE.
- Line 19: `eventNames.cmdExecuting = names.cmdExecuting;` — REMOVE.
- KEPT unchanged: openSettings, openShortcuts, settingsChanged, settingsWindowClosing (lines 4-7) and their initEventNames() assignments (lines 15-18).

From frontend/src/style.css (current state, before this plan):
- Lines 1796-1964: the entire `.output-pane*` CSS block plus the @keyframes output-slide-in / output-slide-out / blink animations. Verify with `grep -n 'output-pane\|output-' frontend/src/` BEFORE removing — should return only the style.css and OutputPane.tsx references. After removing, the grep should return zero.
- KEPT unchanged: the `/* ========== Terminal Split Pane ========== */` block starting at line 1966 (with `.terminal-pane`, `.terminal-container`, etc.) — that is the active terminal CSS, not the dead output pane CSS.

From frontend/src/locales/en.json (current state, before this plan):
- Line 10: `"copyLastOutput": "Copy last output",` (in `common` block) — KEPT (still used by App.tsx:1572 terminal copy button).
- Line 48: `"runInTerminal": "Run in Terminal",` (in `commandDetail` block) — REMOVE (no consumers; verified by grep `t('commandDetail.runInTerminal')` returning zero).
- Lines 167-174: `historyPane` block — REMOVE (no consumers).
- Lines 175-189: `outputPane` block — REMOVE (no consumers; OutputPane deleted in Task 1).

From frontend/e2e/utils/selectors.ts (current state, before this plan):
- Line 40: `outputPane: '[data-testid="output-pane"]',` — REMOVE.
- Line 41: `historyPane: '[data-testid="history-pane"]',` — REMOVE.
- Pre-removal verification: `grep -rn "outputPane\|historyPane" frontend/e2e/tests/` returns zero (no e2e test consumes them).

From frontend/e2e/mocks/runtime.ts (current state, before this plan):
- Line 9: `let executionHistory: any[] = [];` — REMOVE (no remaining consumers after the deleted mocks).
- Line 322-337: `RunCommand` mock — REMOVE the `executionHistory.push(record);` line at 335. Keep the rest of the mock (it returns a record so App.tsx can still call RunCommand).
- Line 339-340: `// RunInTerminal(commandID, variables)` `1736747747: () => {},` — REMOVE.
- Line 343-344: `// GetExecutionHistory` `2752844091: () => executionHistory,` — REMOVE.
- Line 346-349: `// ClearExecutionHistory` `3022740230: () => { executionHistory = []; },` — REMOVE.
- Line 387: `executionHistory = [];` inside ResetAllData mock (line 383-388) — REMOVE.
- Line 415: `executionHistory = [];` inside __cmdexE2E.reset (line 411-417) — REMOVE.
- KEPT unchanged: the `now` helper (line 27) — still used by category/command CRUD timestamps at lines 107, 108, 118, 159, 160, 188, 205.

From frontend/bindings/cmdex/executionservice.js (current state, before this plan):
- Line 22-24: `ClearExecutionHistory` export — will be dropped by regen.
- Line 30-34: `GetExecutionHistory` export — will be dropped by regen.
- Line 67-69: `RunInTerminal` export — will be dropped by regen.
- KEPT unchanged: `GetVariables` (line 41-45) and `RunCommand` (line 55-59).

From frontend/bindings/cmdex/models.js (current state, before this plan):
- Lines 348-402: `EventNames` class with a `cmdExecuting` field at lines 354-360. After regen, this field is dropped.
- KEPT unchanged: all other model classes (Category, Command, ExecutionRecord, VariableDefinition, VariablePrompt, etc.).

Bindings regeneration command (per AGENTS.md and Makefile line 9-10): `wails3 generate bindings` (or `make generate`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terminal.tsx cleanup + App.tsx prop removal + delete OutputPane.tsx</name>
  <files>frontend/src/components/Terminal.tsx, frontend/src/App.tsx, frontend/src/components/OutputPane.tsx</files>
  <read_first>
    - frontend/src/components/Terminal.tsx (full file — current state with the cmd-executing subscription at lines 334-345, activeSessionId prop at line 16, destructuring at line 28, ref at lines 40-43, and eventNames import at line 8)
    - frontend/src/App.tsx (read around lines 1520-1614 — the TerminalTabBar call at 1522-1530 is KEPT, the TerminalComponent call at 1589-1612 has its `activeSessionId={activeSessionId}` line 1601 to remove)
    - frontend/src/components/OutputPane.tsx (full 280-line file to delete)
    - .planning/phases/24-session-aware-execution/24-PATTERNS.md (Terminal.tsx section lines 254-288, App.tsx section lines 339-369, OutputPane.tsx section lines 416-428)
    - .planning/phases/24-session-aware-execution/24-RESEARCH.md (Pitfall 4 lines 416-424)
  </read_first>
  <action>
    Per D-04 ("Remove OutputPane.tsx and all cmd-output event subscriptions"), Pitfall 4 ("Remove both the `const cleanupCmdExecuting = Events.On(...)` block AND the `cleanupCmdExecuting()` line in the return. Also remove the unused `eventNames` import ... if `eventNames.cmdExecuting` was its only remaining use — verify by grepping `eventNames` in `Terminal.tsx` after the edit."), and Pitfall 2 (the OutputPane.tsx pre-delete verification).

    **Step 1: Pre-flight grep to confirm the cleanup is safe.**
    Run these greps BEFORE making any edits. All must return zero (or in the case of the pre-flight check, prove that no imports remain):
    - `grep -rn "from.*OutputPane" frontend/src/` — must return zero matches. The OutputPane.tsx deletion is a deletion of orphan code only safe if no app code imports it.
    - `grep -rn "OutputPane" frontend/src/` — must return only references inside OutputPane.tsx itself (which will be deleted in Step 3). After Step 3, the next grep will return zero.
    - `grep -c "eventNames" frontend/src/components/Terminal.tsx` — should return at least 1 (the import on line 8) plus the single usage on line 334. After Step 2, the import is removed and the only remaining usages are gone. Re-grep at the end of Step 2 must return zero.

    If any of the above return non-zero in a way that suggests a hidden consumer, STOP and resolve.

    **Step 2: Edit frontend/src/components/Terminal.tsx.**
    Remove the cmd-executing subscription and the activeSessionId prop plumbing:
    1. Delete line 8 entirely: `import { eventNames } from '../wails/events';`. The `eventNames` import was only used by the subscription being removed. After this edit, `grep -c "eventNames" frontend/src/components/Terminal.tsx` must return 0.
    2. Edit the `TerminalComponentProps` interface at line 12-18 — drop the `activeSessionId: string;` field. The remaining fields are `isVisible: boolean; theme: string; sessionId: string; onShellExit?: () => void;`.
    3. Edit the `forwardRef` destructuring at line 28 — drop `activeSessionId` from the param list. New destructuring: `({ isVisible, theme, sessionId, onShellExit }, ref) => {`.
    4. Delete lines 40-43 (the `activeSessionIdRef` declaration and the effect that updates it). These were only consumed by the `cleanupCmdExecuting` guard at line 335.
    5. Delete lines 334-345 (the entire `cleanupCmdExecuting = Events.On(eventNames.cmdExecuting, ...)` block). The block is 12 lines including a comment if any and the closing `});` on line 345.
    6. Edit the cleanup return at line 347-352 — remove the `cleanupCmdExecuting();` line at line 351. The remaining cleanup calls (`cleanupOutput()`, `cleanupExit()`, `cleanupCleared()`) are kept.

    After Step 2, `grep -c "activeSessionId" frontend/src/components/Terminal.tsx` must return 0. `grep -c "eventNames" frontend/src/components/Terminal.tsx` must return 0. `grep -c "cleanupCmdExecuting" frontend/src/components/Terminal.tsx` must return 0. `grep -c "term.onData" frontend/src/components/Terminal.tsx` must return at least 1 (Ctrl+C path preserved).

    Do NOT touch:
    - Lines 246-254: `term.onData` → `Write(sessionIdRef.current, data)` — preserved for EXEC-06.
    - Lines 305-318: `pty-output:{sessionId}` subscription — preserved for EXEC-05.
    - Lines 320-328: `pty-exit:{sessionId}` subscription — preserved.
    - Lines 330-332: `pty-cleared:{sessionId}` subscription — preserved.
    - The `useImperativeHandle` block at lines 56-169 — the `clear()`, `getSelection()`, `getLastOutput()`, `focus()` methods are all still consumed by App.tsx (lines 1547, 1559-1567).

    **Step 3: Edit frontend/src/App.tsx.**
    In the `<TerminalComponent>` JSX at lines 1589-1612, remove ONLY the `activeSessionId={activeSessionId}` prop at line 1601. The props to keep are:
    - `key={id}` (line 1590)
    - `ref={...}` (lines 1591-1597)
    - `isVisible={id === activeSessionId && !terminalCollapsed}` (line 1598)
    - `theme={theme}` (line 1599)
    - `sessionId={id}` (line 1598 — actually sessionId is also on line 1600 area; preserve it exactly)
    - `onShellExit={...}` (lines 1602-1611)

    Do NOT touch the `<TerminalTabBar ... activeSessionId={activeSessionId} ... />` call at lines 1522-1530. TerminalTabBar has its own `activeSessionId` prop (separate interface, separate file) and that prop is still used for tab-selection highlighting in TerminalTabBar.tsx:174, 211.

    After Step 3, `grep "activeSessionId={activeSessionId}" frontend/src/App.tsx` must return exactly 1 match (the TerminalTabBar call at line 1524). `grep "sessionId={id}" frontend/src/App.tsx` must return at least 1 match (TerminalComponent still receives sessionId).

    Do NOT touch the `t('common.copyLastOutput')` button at line 1572 — the i18n key is still in use for the terminal copy button (the App.tsx `getLastOutput` + clipboard copy pattern, not the OutputPane pattern).

    Do NOT touch any other reference to `activeSessionId` in App.tsx (e.g., line 177 state declaration, line 226 effect dependency, lines 1285, 1294, 1305, 1326, 1547, 1559, 1598, 1608) — those are all part of the active-session management flow that lives in App.tsx, not the TerminalComponent prop.

    **Step 4: Delete frontend/src/components/OutputPane.tsx.**
    Use the bash `rm` command (or equivalent). The 280-line file is orphan code with no app-side imports (verified by the pre-flight grep in Step 1). Per the AGENTS.md "Surgical Changes" rule, this is a deletion of pre-existing dead code co-located with the Phase 24 refactor — not a drive-by cleanup.

    After deletion, `ls frontend/src/components/OutputPane.tsx` must return "No such file or directory". `grep -c "OutputPane" frontend/src/` must return 0.

    **Step 5: Verify with `pnpm tsc --noEmit`.**
    - `cd frontend && pnpm tsc --noEmit` must exit 0. The cleanup removed the `eventNames` import from Terminal.tsx (since the cmd-executing subscription is gone and the import had no other consumer), so the bindings still have the field (regen happens in Task 2) but the import line is gone — `tsc` will pass as long as Terminal.tsx no longer references the import.
    - At this point, `pnpm tsc --noEmit` may pass even with the stale bindings (since the regenerated bindings regen in Task 2 drops the `CmdExecuting` field, and the deleted subscription in Step 2 no longer references `eventNames.cmdExecuting`). If `pnpm tsc --noEmit` reports a type error related to `eventNames.cmdExecuting` or `RunInTerminal` import, it means a stale import remains — re-grep `frontend/src/` and resolve.
    - Do NOT run `go build ./...` yet — the Go source is unchanged in this plan (Plan 01's edits are intact). The `go build` is the Task 2 final verify.
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/OutputPane.tsx` reports "No such file or directory" (file deleted)
    - `grep -c "OutputPane" frontend/src/` returns 0 (no remaining references in src/)
    - `grep -c "eventNames" frontend/src/components/Terminal.tsx` returns 0 (import removed)
    - `grep -c "cleanupCmdExecuting" frontend/src/components/Terminal.tsx` returns 0 (declaration + cleanup call both removed)
    - `grep -c "activeSessionId" frontend/src/components/Terminal.tsx` returns 0 (prop + ref + effect all removed)
    - `grep -c "term.onData" frontend/src/components/Terminal.tsx` returns at least 1 (Ctrl+C path preserved)
    - `grep -c "pty-output:" frontend/src/components/Terminal.tsx` returns at least 1 (output streaming preserved)
    - `grep -c "pty-exit:" frontend/src/components/Terminal.tsx` returns at least 1 (exit event preserved)
    - `grep -c "pty-cleared:" frontend/src/components/Terminal.tsx` returns at least 1 (clear event preserved)
    - `grep "activeSessionId={activeSessionId}" frontend/src/App.tsx` returns exactly 1 match (the TerminalTabBar at line 1524 — TerminalComponent call at line 1601 is removed)
    - `grep "sessionId={id}" frontend/src/App.tsx` returns at least 1 match (TerminalComponent still receives sessionId)
    - `grep "t('common.copyLastOutput')" frontend/src/App.tsx` returns 1 match (terminal copy button i18n preserved)
    - `cd frontend && pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Terminal.tsx no longer subscribes to cmd-executing event, no longer has the activeSessionId prop, and no longer imports eventNames. App.tsx no longer passes activeSessionId to TerminalComponent (TerminalTabBar still gets it). OutputPane.tsx is deleted. pnpm tsc passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead i18n keys + CSS + e2e mocks + regenerate Wails bindings</name>
  <files>frontend/src/wails/events.ts, frontend/src/style.css, frontend/src/locales/en.json, frontend/e2e/utils/selectors.ts, frontend/e2e/mocks/runtime.ts, frontend/bindings/cmdex/executionservice.js, frontend/bindings/cmdex/eventservice.js, frontend/bindings/cmdex/models.js</files>
  <read_first>
    - frontend/src/wails/events.ts (full file — current state with cmdExecuting at line 8 and line 19)
    - frontend/src/style.css (lines 1796-1964: the .output-pane* block to remove; lines 1966+: the active terminal CSS to keep)
    - frontend/src/locales/en.json (lines 1-50: commandDetail block with runInTerminal at line 48; lines 167-189: historyPane and outputPane blocks; line 10: common.copyLastOutput to KEEP)
    - frontend/e2e/utils/selectors.ts (lines 39-41: the output/history pane selectors to remove)
    - frontend/e2e/mocks/runtime.ts (lines 1-50: imports + state; lines 322-350: RunCommand + deleted method mocks; lines 383-388: ResetAllData; lines 410-417: __cmdexE2E.reset)
    - frontend/bindings/cmdex/executionservice.js (current state — 75 lines with 5 exported functions; expected to drop to 50 lines after regen)
    - frontend/bindings/cmdex/eventservice.js (current state — unchanged structure expected)
    - frontend/bindings/cmdex/models.js (lines 345-402: EventNames class to drop the cmdExecuting field from)
    - .planning/phases/24-session-aware-execution/24-PATTERNS.md (events.ts section, style.css section, locales/en.json section, selectors.ts section, runtime.ts section)
    - .planning/phases/24-session-aware-execution/24-RESEARCH.md (Pitfall 2 lines 392-404, Pitfall 3 lines 406-414, Runtime State Inventory lines 519-535)
  </read_first>
  <action>
    Per D-04 ("Remove OutputPane.tsx and all cmd-output event subscriptions"), the Open Question #2/#3 resolutions from RESEARCH.md ("delete the orphan CSS and i18n in the same commit as the component"), and the binding regen requirement from Pitfall 3 ("Grep for `RunInTerminal` across `frontend/src/` before regenerating. The current state shows zero imports in app code — the method is exposed but unused. After regeneration, no further action is needed.")

    **Pre-flight verification (do these FIRST, all must return zero or pass before edits):**
    - `grep -rn "outputPane\b" frontend/src/` returns zero — verify no `t('outputPane.XXX')` lookups remain (the OutputPane.tsx file deletion in Task 1 already enforces the `from.*OutputPane` check).
    - `grep -rn "historyPane\b" frontend/src/` returns zero — verify no `t('historyPane.XXX')` lookups remain.
    - `grep -rn "commandDetail.runInTerminal\|t('commandDetail.runInTerminal')" frontend/src/` returns zero.
    - `grep -rn "RunInTerminal\|GetExecutionHistory\|ClearExecutionHistory" frontend/src/` returns zero — verify no stale imports of the soon-to-be-regenerated bindings.
    - `grep -rn "outputPane\|historyPane" frontend/e2e/tests/` returns zero — verify no e2e test consumes the selectors.

    If any of the above return non-zero, STOP and resolve before continuing.

    **Step 1: Edit frontend/src/wails/events.ts.**
    - Remove line 8: `    cmdExecuting: 'cmd-executing',` (3-space indent + key + comma).
    - Remove line 19: `        eventNames.cmdExecuting = names.cmdExecuting;` (8-space indent + statement + semicolon).
    - Verify after edit: `grep -c cmdExecuting frontend/src/wails/events.ts` returns 0.

    **Step 2: Edit frontend/src/style.css.**
    - Delete the entire CSS block from the `/* ========== Output Pane ========== */` comment at line 1796 through the end of the `@keyframes output-slide-out` block at line 1964. The block to remove includes:
      - The header comment line 1796.
      - All `.output-pane*` rules.
      - The `.output-cmd-prefix`, `.output-copy-inline`, `.output-copy-cmd`, `.output-empty-response`, `.output-cursor`, `.output-empty`, `.output-collapse-btn`, `.terminal-dots`, `.terminal-dot`, `.output-resize-handle` rules (lines 1798-1926).
      - The `@keyframes blink` rule (line 1928).
      - The `.output-pane [data-radix-collapsible-content]` rules (lines 1930-1942).
      - The `@keyframes output-slide-in` and `@keyframes output-slide-out` rules (lines 1944-1964).
    - The line after the deletion should be either the `/* ========== Terminal Split Pane ========== */` comment (currently line 1966) or whatever immediately preceded line 1796 (currently `}` at line 1794 closing a previous block). The simplest implementation: delete from line 1796 to line 1964 inclusive, leaving a single blank line between the previous block's closing `}` and the `/* ========== Terminal Split Pane ========== */` comment.
    - After edit, `grep -n "output-pane\|output-slide\|output-cursor" frontend/src/style.css` returns zero.
    - Verify the `/* ========== Terminal Split Pane ========== */` block (line 1966+) is intact — `grep -c ".terminal-pane" frontend/src/style.css` returns at least 1.

    **Step 3: Edit frontend/src/locales/en.json.**
    - Remove line 48: `    "runInTerminal": "Run in Terminal",` (4-space indent + key + value + comma, in the `commandDetail` block).
    - Remove lines 167-174 (the entire `historyPane` block, 8 lines including the closing `}`). The block starts with `  "historyPane": {` at line 167 and ends with `  }` at line 174.
    - Remove lines 175-189 (the entire `outputPane` block, 15 lines including the closing `}`). The block starts with `  "outputPane": {` at line 175 and ends with `  }` at line 189.
    - After edit, `grep -c "outputPane\|historyPane\|runInTerminal" frontend/src/locales/en.json` returns zero.
    - Verify `grep -c "copyLastOutput" frontend/src/locales/en.json` returns 1 (the terminal copy button key is preserved).
    - Verify the file is still valid JSON: `python3 -c "import json; json.load(open('frontend/src/locales/en.json'))"` exits 0 (or use any equivalent JSON validator). A trailing-comma error here would break the i18n loader.

    **Step 4: Edit frontend/e2e/utils/selectors.ts.**
    - Remove line 40: `  outputPane: '[data-testid="output-pane"]',` (2-space indent + key + value + comma).
    - Remove line 41: `  historyPane: '[data-testid="history-pane"]',` (2-space indent + key + value + comma).
    - After edit, `grep -c "outputPane\|historyPane" frontend/e2e/utils/selectors.ts` returns 0.

    **Step 5: Edit frontend/e2e/mocks/runtime.ts.**
    - Remove line 9: `let executionHistory: any[] = [];`
    - In the `RunCommand` mock at lines 322-337, remove the line `    executionHistory.push(record);` (line 335). The mock still returns the constructed record (App.tsx still calls RunCommand and reads `result.error` and `result.exitCode`).
    - Remove the entire `// RunInTerminal` block at lines 339-340 (the comment + the `1736747747: () => {},` entry).
    - Remove the entire `// ── History ──` section at lines 342-349 (the section comment + the `// GetExecutionHistory` 2752844091 entry + the `// ClearExecutionHistory` 3022740230 entry).
    - In the `ResetAllData` mock at lines 383-388, remove the line `    executionHistory = [];`.
    - In the `__cmdexE2E.reset` function at lines 411-417, remove the line `    executionHistory = [];`.
    - After edit, `grep -c "executionHistory\|RunInTerminal\|GetExecutionHistory\|ClearExecutionHistory" frontend/e2e/mocks/runtime.ts` returns 0.
    - Verify `now()` helper at line 27 is still defined and still used by category/command CRUD — `grep -c "now()" frontend/e2e/mocks/runtime.ts` returns at least 5 (it's used at lines 107, 108, 118, 159, 160, 188, 205; after edit all 7 should still be there).

    **Step 6: Regenerate Wails bindings.**
    Run `wails3 generate bindings` (this is the Makefile target `make generate` per AGENTS.md line 56-57 and the Makefile line 9-10). This regenerates:
    - `frontend/bindings/cmdex/executionservice.js` — drops the `RunInTerminal`, `GetExecutionHistory`, `ClearExecutionHistory` exports. The file should drop from 75 lines to ~50 lines (5 exported functions → 2).
    - `frontend/bindings/cmdex/eventservice.js` — structural shape unchanged (only the model reference changes).
    - `frontend/bindings/cmdex/models.js` — `EventNames` class drops the `cmdExecuting` field. The `ExecutionRecord` class is unchanged.

    If the command is not available in the executor's environment, fall back to `make generate` (which is `wails3 generate bindings` per the Makefile). If neither is available, the executor should report the missing tool to the user — the bindings regen is a hard requirement for `pnpm tsc --noEmit` to pass after this plan (the regenerated `models.js` `EventNames` class drops the `cmdExecuting` field, and any stale TS import of it would fail type checking).

    **Step 7: Verify with `pnpm tsc --noEmit && go build ./...`.**
    - `pnpm tsc --noEmit` must exit 0. The regenerated bindings (smaller executionservice.js, EventNames without cmdExecuting) must match the frontend's expectations (no remaining imports of the deleted symbols, no remaining references to the dropped field).
    - `go build ./...` must exit 0 — the Go side is unchanged in this plan (Plan 01's edits are intact), but the build is the safety net that confirms nothing got accidentally reverted.
    - If `pnpm tsc --noEmit` reports `Module '"../../bindings/cmdex/executionservice"' has no exported member 'RunInTerminal'`, that means a stale import remains in `frontend/src/`. Re-run the pre-flight grep and resolve.
    - If `pnpm tsc --noEmit` reports a type error related to `eventNames.cmdExecuting` or `names.cmdExecuting`, that means a stale reference remains. Re-grep `frontend/src/` and resolve.
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cmdExecuting" frontend/src/wails/events.ts` returns 0 (const map entry + initEventNames assignment both removed)
    - `grep -c "cmdExecuting" frontend/bindings/cmdex/models.js` returns 0 (regenerated EventNames class has no cmdExecuting field)
    - `grep -c "RunInTerminal\|GetExecutionHistory\|ClearExecutionHistory" frontend/bindings/cmdex/executionservice.js` returns 0 (3 methods dropped from regenerated bindings)
    - `grep -c "output-pane\|output-slide\|output-cursor" frontend/src/style.css` returns 0 (dead CSS removed)
    - `grep -c ".terminal-pane" frontend/src/style.css` returns at least 1 (active terminal CSS preserved)
    - `grep -c "outputPane\|historyPane\|runInTerminal" frontend/src/locales/en.json` returns 0 (3 dead i18n entries removed)
    - `grep -c "copyLastOutput" frontend/src/locales/en.json` returns 1 (terminal copy button i18n preserved)
    - `python3 -c "import json; json.load(open('frontend/src/locales/en.json'))"` exits 0 (JSON still valid)
    - `grep -c "outputPane\|historyPane" frontend/e2e/utils/selectors.ts` returns 0 (2 dead selectors removed)
    - `grep -c "executionHistory\|RunInTerminal\|GetExecutionHistory\|ClearExecutionHistory" frontend/e2e/mocks/runtime.ts` returns 0 (5 references removed)
    - `grep -c "now()" frontend/e2e/mocks/runtime.ts` returns at least 5 (now() helper still used for category/command timestamps)
    - `cd frontend && pnpm tsc --noEmit` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>events.ts no longer references cmdExecuting; style.css has the .output-pane* block removed (the .terminal-pane block is intact); en.json has the runInTerminal/historyPane/outputPane keys removed (common.copyLastOutput preserved); e2e selectors.ts has outputPane/historyPane removed; e2e/mocks/runtime.ts has the 3 deleted method mocks and the executionHistory state removed. Wails bindings regenerated: executionservice.js drops 3 exports, models.js EventNames drops the cmdExecuting field. pnpm tsc and go build both pass with zero errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend ↔ Backend (Wails IPC) | Generated bindings (executionservice.js, eventservice.js, models.js) form the contract between React frontend and Go services. Regeneration must match the Go source after Plan 01. |
| E2E test mocks ↔ real runtime | e2e/mocks/runtime.ts is a Playwright-only in-memory backend. Its handlers must mirror the deleted Go methods (drops RunInTerminal/GetExecutionHistory/ClearExecutionHistory) so e2e tests don't import non-existent handlers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Tampering (Bindings Drift) | Stale import of `RunInTerminal` or `eventNames.cmdExecuting` after bindings regen | mitigate | Pre-flight grep + post-edit grep for `RunInTerminal` and `cmdExecuting` in `frontend/src/`. pnpm tsc --noEmit catches any remaining stale import. |
| T-24-07 | Tampering (CSS Orphan) | `.output-pane*` rules left in style.css with no DOM target | accept | Pure dead-CSS — no runtime impact, no security surface. Removing for code hygiene per Open Question #2. |
| T-24-08 | Information Disclosure | `t('common.copyLastOutput')` i18n key removal | accept | The terminal copy button at App.tsx:1572 still uses this key. Verified preserved. |
| T-24-09 | Denial of Service (E2E Mocks) | e2e mock with stale method IDs (e.g., 1736747747 for RunInTerminal still in handlers map) | mitigate | The e2e mock handlers map uses method IDs as keys. Removing the handlers with IDs 1736747747/2752844091/3022740230 prevents accidental calls. The `Call.ByID` mock in line 391-404 warns on unknown IDs but doesn't fail — the e2e test suite would not be broken by orphan entries, but removing them prevents future confusion. |
| T-24-10 | Tampering (Test Isolation) | e2e mock state leak via executionHistory | mitigate | Removing the `executionHistory` array entirely ensures no state from a test run leaks into another. Mirrors D-09 (no history persistence for terminal runs) at the mock level. |

**Assessment:** Negligible security surface change. All removals are deletions of dead code co-located with the Phase 24 refactor. The bindings regen is the only "write" operation, and it's a re-derivation from the Go source (Plan 01's output).
</threat_model>

<verification>
After both tasks complete:
1. `cd frontend && pnpm tsc --noEmit` exits 0 — frontend type check passes with the regenerated bindings
2. `go build ./...` exits 0 — Go build is clean (no accidental regression)
3. `ls frontend/src/components/OutputPane.tsx` returns "No such file or directory" — the orphan file is gone
4. `grep -c "OutputPane" frontend/src/` returns 0 — no remaining app-side references
5. `grep -c "eventNames" frontend/src/components/Terminal.tsx` returns 0 — the import was the only consumer, now gone
6. `grep -c "RunInTerminal\|GetExecutionHistory\|ClearExecutionHistory" frontend/bindings/cmdex/executionservice.js` returns 0 — regenerated bindings dropped 3 exports
7. `grep -c "cmdExecuting" frontend/bindings/cmdex/models.js` returns 0 — regenerated EventNames class has no cmdExecuting field
8. `grep -c "output-pane\|output-slide\|output-cursor" frontend/src/style.css` returns 0 — dead CSS removed
9. `grep -c "outputPane\|historyPane\|runInTerminal" frontend/src/locales/en.json` returns 0 — dead i18n removed
10. `grep -c "copyLastOutput" frontend/src/locales/en.json` returns 1 — terminal copy button i18n preserved
11. `grep -c "executionHistory\|RunInTerminal\|GetExecutionHistory\|ClearExecutionHistory" frontend/e2e/mocks/runtime.ts` returns 0 — e2e mock state and handlers cleaned
12. `grep "activeSessionId={activeSessionId}" frontend/src/App.tsx` returns exactly 1 — TerminalTabBar still gets it; TerminalComponent doesn't
</verification>

<success_criteria>
- [x] D-04: cmd-executing event subscription removed from Terminal.tsx; OutputPane.tsx deleted; cmd-output subscriptions are gone (already removed in Phase 23)
- [x] D-04: CmdExecuting field removed from frontend (events.ts + regenerated bindings)
- [x] ActiveSessionId prop removed from TerminalComponent; App.tsx no longer passes it to the TerminalComponent call site
- [x] All dead i18n keys (runInTerminal, historyPane.*, outputPane.*) removed
- [x] Dead CSS (.output-pane* block, ~170 lines) removed; active .terminal-pane CSS preserved
- [x] Dead e2e selectors (outputPane, historyPane) removed
- [x] Dead e2e mock handlers (RunInTerminal, GetExecutionHistory, ClearExecutionHistory) and state (executionHistory) removed
- [x] Wails bindings regenerated to match Plan 01's Go changes
- [x] common.copyLastOutput i18n key preserved (still used by terminal copy button)
- [x] TerminalTabBar still receives activeSessionId (its own prop, separate component)
- [x] Ctrl+C path (term.onData → Write(sessionId, "\x03")) preserved for EXEC-06
- [x] Output streaming (pty-output:{sessionId} subscription) preserved for EXEC-05
- [x] pnpm tsc --noEmit and go build ./... both pass
</success_criteria>

<output>
Create `.planning/phases/24-session-aware-execution/24-02-SUMMARY.md` when done
</output>
