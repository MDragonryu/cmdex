---
phase: 21-backend-session-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - frontend/src/wails/events.ts
  - frontend/src/components/Terminal.tsx
  - frontend/bindings/cmdex/terminalservice/
  - frontend/bindings/cmdex/eventservice/
  - terminal_service_test.go
  - execution_service_test.go
autonomous: true
requirements:
  - SESS-01
  - SESS-04
  - SESS-05
  - EXEC-04

must_haves:
  truths:
    - "Frontend eventNames object has no ptyOutput, ptyExit, or ptyCleared fields"
    - "TerminalComponent accepts a sessionId prop and constructs namespaced event names from it"
    - "TerminalComponent subscribes to pty-output:{sessionId}, pty-exit:{sessionId}, pty-cleared:{sessionId}"
    - "TerminalComponent passes sessionId to all Wails method calls (Write, Resize, Clear, Start)"
    - "Wails bindings are regenerated and compatible with new Go method signatures"
    - "go test -race -count=1 -v ./... passes all multi-session tests"
    - "CreateSession test verifies UUID format, default name, and running=true"
    - "Process persistence test verifies long-running process survives active session switch"
    - "Output isolation test verifies session A output does not appear in session B"
    - "Concurrent access test passes under race detector"
  artifacts:
    - path: "frontend/src/wails/events.ts"
      provides: "Event names object without PTY constants"
      contains: "export const eventNames"
    - path: "frontend/src/components/Terminal.tsx"
      provides: "Session-aware terminal with namespaced event subscriptions"
      contains: '"pty-output:" + sessionId'
    - path: "terminal_service_test.go"
      provides: "Multi-session test coverage"
      contains: "TestTerminalService_CreateSession"
    - path: "execution_service_test.go"
      provides: "Updated singleton test"
      contains: "TestTerminalService_ServiceStartupAssignsTerminalSvc"
  key_links:
    - from: "Terminal.tsx Events.On"
      to: 'event name "pty-output:" + sessionId'
      via: "template literal"
      pattern: '`pty-output:\$\{sessionId\}`'
    - from: "Terminal.tsx Write call"
      to: "terminalservice binding"
      via: "Write(sessionId, data)"
      pattern: "Write\\(sessionId,"
    - from: "terminal_service_test.go"
      to: "terminal_service.go CreateSession"
      via: "s.CreateSession() returns *SessionInfo"
      pattern: "CreateSession\\(\\)"
</objective>

<objective>
Update the frontend to support namespaced session events per D-04, regenerate Wails bindings for the new Go method signatures, and add comprehensive multi-session tests with race detection per the validation strategy.

Purpose: Completes the Phase 21 contract — all four requirements (SESS-01, SESS-04, SESS-05, EXEC-04) are verified through automated tests. The frontend wiring enables Phase 23's tabbed terminal UI to subscribe to per-session events.
Output: Updated events.ts and Terminal.tsx, regenerated bindings, expanded test coverage in terminal_service_test.go and execution_service_test.go.
</objective>

<execution_context>
@/Users/mac/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/mac/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@frontend/src/wails/events.ts (current — has ptyOutput/ptyExit/ptyCleared to remove)
@frontend/src/components/Terminal.tsx (current — uses eventNames.ptyOutput, calls Write/Resize/Clear/Start without sessionId)
@terminal_service_test.go (current — 10 single-session tests to preserve and expand)
@execution_service_test.go (lines 176-188 — singleton test to update)
@.planning/phases/21-backend-session-foundation/21-CONTEXT.md (D-04 namespaced events, D-03 hybrid active session)
@.planning/phases/21-backend-session-foundation/21-PATTERNS.md (sections 6-7: Terminal.tsx and events.ts exact changes)
@.planning/phases/21-backend-session-foundation/21-VALIDATION.md (test-to-requirement map, sampling rates)
@.planning/phases/21-backend-session-foundation/21-RESEARCH.md (open question 1 about Terminal.tsx scope, A2 assumption)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update frontend events.ts, Terminal.tsx, and regenerate Wails bindings</name>
  <files>frontend/src/wails/events.ts, frontend/src/components/Terminal.tsx, frontend/bindings/cmdex/terminalservice/, frontend/bindings/cmdex/eventservice/</files>
  <read_first>
    - frontend/src/wails/events.ts (lines 1-30: eventNames object with ptyOutput/ptyExit/ptyCleared to remove + initEventNames)
    - frontend/src/components/Terminal.tsx (full file: lines 1-354 — props, imperative handle, useEffect with event subscriptions, method calls)
    - .planning/phases/21-backend-session-foundation/21-PATTERNS.md (lines 556-668: exact Terminal.tsx and events.ts changes with code excerpts)
    - .planning/phases/21-backend-session-foundation/21-UI-SPEC.md (lines 111-135: Phase-specific event wiring changes)
  </read_first>
  <action>
    Per D-04 from CONTEXT.md (namespaced events, clean break from globals). Follow patterns from PATTERNS.md sections 6-7.

    **STEP 1: Update `frontend/src/wails/events.ts`**
    - Remove `ptyOutput: 'pty-output'` from `eventNames` object (line 8).
    - Remove `ptyExit: 'pty-exit'` from `eventNames` object (line 9).
    - Remove `ptyCleared: 'pty-cleared'` from `eventNames` object (line 10).
    - In `initEventNames()` function: remove `eventNames.ptyOutput = names.ptyOutput` (line 22), `eventNames.ptyExit = names.ptyExit` (line 23), `eventNames.ptyCleared = names.ptyCleared` (line 24).
    - Keep all other fields (openSettings, openShortcuts, settingsChanged, settingsWindowClosing, cmdExecuting) as-is.

    **STEP 2: Regenerate Wails frontend bindings**
    - Run `wails3 generate build-assets` to update `frontend/bindings/cmdex/terminalservice/` and `frontend/bindings/cmdex/eventservice/`.
    - This regenerates TypeScript types for: Write(sessionId, data), Resize(sessionId, cols, rows), Clear(sessionId), Start(sessionId, cols, rows), Stop(sessionId), CreateSession(), ListSessions(), CloseSession(id), RenameSession(id, name), SetActiveSession(id), GetActiveSession().
    - Also regenerates EventNames type without PtyOutput/PtyExit/PtyCleared.
    - Verify `frontend/bindings/cmdex/terminalservice/` now contains `.ts` and `.d.ts` files with sessionId parameters.

    **STEP 3: Update `frontend/src/components/Terminal.tsx`**
    
    a. Update imports (lines 8-9):
       - Remove `import { eventNames } from '../wails/events'` — the import is still needed for `eventNames.cmdExecuting` (used on line 290).
       - Keep `import { Write, Resize, Start, Clear } from '../../bindings/cmdex/terminalservice'` — the regenerated bindings will have these with sessionId.
    
    b. Update `TerminalComponentProps` interface (lines 12-16):
       - Add `sessionId: string;` field between `theme` and `onShellExit`.

    c. Update `TerminalComponent` function signature (line 25):
       - Destructure `sessionId` from props: `({ isVisible, theme, sessionId, onShellExit }, ref)`.

    d. Update imperative handle Clear (lines 44-56):
       - Change `Clear()` to `Clear(sessionId)`.
    
    e. Update terminal start (line 223):
       - Change `Start(term.cols, term.rows)` to `Start(sessionId, term.cols, term.rows)`.

    f. Update Write (line 240):
       - Change `Write(data)` to `Write(sessionId, data)`.

    g. Update Resize (line 250):
       - Change `Resize(cols, rows)` to `Resize(sessionId, cols, rows)`.

    h. Update event subscriptions (lines 265-300) — replace global event references with namespaced:
       - Construct event name strings at the top of the useEffect (after `terminalRef.current = term`):
         ```typescript
         const ptyOutputEvent = "pty-output:" + sessionId;
         const ptyExitEvent = "pty-exit:" + sessionId;
         const ptyClearedEvent = "pty-cleared:" + sessionId;
         ```
       - `cleanupOutput`: Change `Events.On(eventNames.ptyOutput, ...)` to `Events.On(ptyOutputEvent, ...)`.
       - `cleanupExit`: Change `Events.On(eventNames.ptyExit, ...)` to `Events.On(ptyExitEvent, ...)`.
       - `cleanupCleared`: Change `Events.On(eventNames.ptyCleared, ...)` to `Events.On(ptyClearedEvent, ...)`.
       - `cleanupCmdExecuting` (line 290): Keep using `eventNames.cmdExecuting` (still a global event). But update `Write(cmdLine)` to `Write(sessionId, cmdLine)`.

    i. Add `ptyOutputEvent`, `ptyExitEvent`, `ptyClearedEvent` to the cleanup return (lines 302-315) — already handled because `cleanupOutput()`, `cleanupExit()`, `cleanupCleared()` variables still exist, they just subscribe to different event names.

    j. Add `sessionId` to the useEffect dependency array on line 316: change `[]` to `[sessionId]`.

    k. The `eventNames` import (line 8) should remain since it's still used for `eventNames.cmdExecuting` on line 290. But if the only remaining usage is `cmdExecuting`, the import can be kept as-is — the regenerated bindings won't have removed fields.

    **STEP 4: Verify frontend compiles**
    - Run `cd frontend && pnpm tsc --noEmit` to check for type errors.
    - Fix any type errors from regenerated bindings.
  </action>
  <acceptance_criteria>
    - `frontend/src/wails/events.ts` has NO `ptyOutput`, `ptyExit`, `ptyCleared` in `eventNames` object
    - `frontend/src/wails/events.ts` `initEventNames()` has NO assignments to `eventNames.ptyOutput`, `.ptyExit`, `.ptyCleared`
    - `frontend/bindings/cmdex/terminalservice/` contains regenerated `.ts` files with sessionId parameters
    - `frontend/bindings/cmdex/eventservice/` contains regenerated `.ts` files without PtyOutput/PtyExit/PtyCleared
    - `Terminal.tsx` `TerminalComponentProps` has `sessionId: string`
    - `Terminal.tsx` constructs `"pty-output:" + sessionId`, `"pty-exit:" + sessionId`, `"pty-cleared:" + sessionId`
    - `Terminal.tsx` calls `Write(sessionId, data)`, `Resize(sessionId, cols, rows)`, `Clear(sessionId)`, `Start(sessionId, cols, rows)`
    - `Terminal.tsx` `Events.On(eventNames.ptyOutput, ...)` is REPLACED with `Events.On(ptyOutputEvent, ...)`
    - `Terminal.tsx` `useEffect` dependency array includes `sessionId`
    - `cd frontend && pnpm tsc --noEmit` passes with zero errors
  </acceptance_criteria>
  <verify>
    <automated>
cd frontend && pnpm tsc --noEmit
grep -c 'ptyOutput\|ptyExit\|ptyCleared' src/wails/events.ts | grep -q '^0$'
grep -c '"pty-output:" + sessionId' src/components/Terminal.tsx | grep -q '^1$'
grep -c 'Write(sessionId' src/components/Terminal.tsx | grep -q '^1$'
    </automated>
  </verify>
  <done>Frontend event wiring uses namespaced events. TerminalComponent accepts sessionId prop. Wails bindings regenerated with new signatures. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-session Go tests and update execution_service_test.go</name>
  <files>terminal_service_test.go, execution_service_test.go</files>
  <read_first>
    - terminal_service_test.go (all 214 lines: existing 10 single-session tests, helper functions newTestTerminalService and mustStart)
    - execution_service_test.go (lines 176-188: TestTerminalService_ServiceStartupAssignsTerminalSvc)
    - terminal_service.go (result of Plan 02 — SessionInfo, sessionState, CRUD, PTY lifecycle, namespaced events)
    - .planning/phases/21-backend-session-foundation/21-VALIDATION.md (lines 40-49: test-to-requirement map with exact test names and commands)
    - .planning/phases/21-backend-session-foundation/21-PATTERNS.md (lines 438-524: test helper and multi-session test patterns)
    - .planning/phases/21-backend-session-foundation/21-RESEARCH.md (lines 666-700: validation architecture, Phase Requirements → Test Map)
  </read_first>
  <action>
    Follow test patterns from PATTERNS.md section 4 and VALIDATION.md test map. All new tests must use `t.Helper()`, `t.Cleanup`, and `testing.Short()` skip for integration tests. **Race detector is mandatory**: always run with `-race` flag.

    **STEP 1: Update test helpers in `terminal_service_test.go`**

    a. Keep `newTestTerminalService` (already returns `*TerminalService`) but update it to initialize the sessions map:
       - Add `s.sessions = make(map[string]*sessionState)` after creating `&TerminalService{}`.
       - This ensures all test helpers have a valid sessions map.

    b. Fix `mustStart` helper (line 15-23): the old `s.Start(cols, rows)` needs to become `s.Start(sessionId, cols, rows)`. But the helper doesn't know a sessionId.
       - Update `mustStart` to accept `sessionId string` as first param: `mustStart(t, s, sessionId, cols, rows)`.
       - Or: create a new helper `mustCreateAndStart(t, s) string` that calls CreateSession and returns the sessionId.

    c. Add `mustCreateAndStart(t, s) string` helper:
       ```go
       func mustCreateAndStart(t *testing.T, s *TerminalService) string {
           t.Helper()
           info, err := s.CreateSession()
           if err != nil {
               t.Fatalf("CreateSession failed: %v", err)
           }
           t.Cleanup(func() {
               s.CloseSession(info.ID)
           })
           return info.ID
       }
       ```

    d. Update ALL existing 10 single-session tests to work with the new multi-session API. Each test that accessed `s.ptmx`, `s.cmd`, `s.stopCh`, `s.shellPath`, `s.shellFlag`, `s.lastSize`, `s.running` directly must be updated:
       - `TestTerminalBatching` (line 62-70): `s.lastSize` is gone. Update to create a session and check `ss.lastSize`.
       - `TestTerminalStart` (line 72-95): `s.ptmx`, `s.cmd`, `s.cmd.Process` are gone. Update to create session and verify session's PTY started.
       - `TestTerminalWrite` (line 97-110): `mustStart(t, s, 80, 24)` → `mustCreateAndStart(t, s)`, then `s.Write(id, "echo test\n")`.
       - `TestTerminalResize` (line 112-130): Same pattern, check `Resize(id, 120, 40)`.
       - `TestTerminalShutdown` (line 132-179): This test manually sets `s.shellPath`, `s.shellFlag`, `s.ptmx`, `s.cmd`, `s.stopCh`. These fields moved to `sessionState`. Update to: create session, access `ss := s.sessions[id]`, manually set shell/ptmx/cmd on the sessionState, then test `s.Stop(id)`.
       - `TestTerminalExit` (line 181-214): Same pattern — update to use sessionState fields via CreateSession.
       - `TestTerminalDetectShell` (line 26-60): Unchanged — this tests `detectShell()` standalone function.

    **STEP 2: Add new multi-session tests per VALIDATION.md test map**

    a. **`TestTerminalService_CreateSession`** (SESS-01):
       - Create TerminalService with sessions map.
       - Call `CreateSession()`. Verify: `info.ID != ""`, `info.Name == "Terminal 1"`, `info.Running == true` (PTY was started).
       - Verify UUID format: check len(info.ID) and that it contains hyphens.
       - Call `CreateSession()` again. Verify second session: `info.Name == "Terminal 2"`, different ID.
       - Verify `ListSessions()` returns 2 sessions.
       - Cleanup: `s.ServiceShutdown()`.

    b. **`TestTerminalService_ListSessions`** (SESS-01):
       - Create 3 sessions. Verify `ListSessions()` returns exactly 3 with correct names.
       - Close one session. Verify `ListSessions()` returns 2.
       - Cleanup: `s.ServiceShutdown()`.

    c. **`TestTerminalService_RenameSession`** (SESS-04):
       - Create session. Call `RenameSession(id, "New Name")`. Verify `ListSessions()` shows new name.
       - Call `RenameSession(id, "")`. Verify error returned with "name cannot be empty".
       - Call `RenameSession("nonexistent-id", "x")`. Verify error returned with "session not found".
       - Cleanup: `s.ServiceShutdown()`.

    d. **`TestTerminalService_CloseSession`** (SESS-05):
       - Create 2 sessions. Close session A. Verify `ListSessions()` returns only session B.
       - Verify CloseSession on closed session: `CloseSession(closedID)` returns "session not found" error.
       - Verify active session reassignment: create sessions A and B, set active to A, close A, verify `GetActiveSession()` returns session B.
       - Cleanup: `s.ServiceShutdown()`.

    e. **`TestTerminalService_ProcessPersistAcrossSessionSwitch`** (EXEC-04):
       - Integration test: needs `testing.Short()` guard.
       - Create session A and session B (both have running PTYs).
       - Write `sleep 30 &\n` to session A to start a background long-running process in that session.
       - Write `echo "in session B"\n` to session B.
       - Read output from both sessions to verify both are alive.
       - Close session B. Verify session A's PTY is still running (check `ss.running`).
       - Cleanup: `s.ServiceShutdown()`.

    f. **`TestTerminalService_OutputIsolation`** (EXEC-04):
       - Needs `testing.Short()` guard.
       - Create sessions A and B. Write `echo "AAA"\n` to session A, `echo "BBB"\n` to session B.
       - Drain output channels for both sessions.
       - Verify session A's output channel buffer contains "AAA" but NOT "BBB".
       - Verify session B's output channel buffer contains "BBB" but NOT "AAA".
       - Cleanup: `s.ServiceShutdown()`.

    g. **`TestTerminalService_NamespacedEvents`**:
       - Needs `testing.Short()` guard if using actual PTY.
       - Verify that the event emission in emitter flush uses `"pty-output:" + sessionId` format (by checking sessionState's emitter goroutine pattern, or by capturing events with a test hook).
       - Simpler approach: verify that `ss.id` appears in the emission string format (code review-level — confirmed by grep for pattern).

    h. **`TestTerminalService_ShutdownCleansAll`**:
       - Create 3 sessions. Verify 3 in map.
       - Call `ServiceShutdown()`. Verify `len(s.sessions) == 0`.
       - Verify no error from ServiceShutdown.

    i. **`TestTerminalService_ConcurrentAccess`** (race detector):
       - No integration guard needed (this is a pure unit test for map access).
       - Create 1 session.
       - Spawn 5 goroutines that call `ListSessions()` in a loop.
       - Spawn 3 goroutines that call `GetActiveSession()` in a loop.
       - Spawn 1 goroutine that creates and closes a session in a loop.
       - Use `sync.WaitGroup` to coordinate. Verify no panic.
       - This test MUST be run with `-race`: `go test -race -run TestTerminalService_ConcurrentAccess`.

    **STEP 3: Update `execution_service_test.go`**

    - `TestTerminalService_ServiceStartupAssignsTerminalSvc` (lines 176-188):
      - Keep the test structure (save/restore terminalSvc global).
      - After `ServiceStartup(nil, application.ServiceOptions{})`, add: verify `len(s.sessions) == 1` (a default session was created).
      - Add: verify `s.sessionCounter == 1`.
      - Change `defer s.Stop()` to `defer s.ServiceShutdown()`.
      - Verify `terminalSvc != nil` still holds.

    **STEP 4: Verify all tests pass with race detector**
    - Run `go test -race -count=1 -run 'TestTerminal' -v ./...`.
    - Run `go test -race -count=1 -v ./...` for full suite.
    - Fix any failures or race detector warnings.
  </action>
  <acceptance_criteria>
    - `terminal_service_test.go` has `newTestTerminalService` that initializes `s.sessions` map
    - `terminal_service_test.go` has `mustCreateAndStart(t, s)` helper
    - All 10 existing tests pass with the new multi-session API (no direct field access on TerminalService)
    - `TestTerminalService_CreateSession` passes: UUID format, "Terminal 1", running=true, second session "Terminal 2"
    - `TestTerminalService_ListSessions` passes: returns correct count after create/close
    - `TestTerminalService_RenameSession` passes: non-empty succeeds, empty fails, nonexistent ID fails
    - `TestTerminalService_CloseSession` passes: session removed, active reassigned, double-close errors
    - `TestTerminalService_ProcessPersistAcrossSessionSwitch` passes: session A alive after switching to B
    - `TestTerminalService_OutputIsolation` passes: session A output not in session B
    - `TestTerminalService_ShutdownCleansAll` passes: ServiceShutdown empties sessions map
    - `TestTerminalService_ConcurrentAccess` passes under `-race` with no race detector warnings
    - `TestTerminalService_ServiceStartupAssignsTerminalSvc` passes: verifies `sessions[1]` exists
    - `go test -race -count=1 -v ./...` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>
go test -race -count=1 -run 'TestTerminalService_CreateSession|TestTerminalService_ListSessions|TestTerminalService_RenameSession|TestTerminalService_CloseSession|TestTerminalService_ShutdownCleansAll|TestTerminalService_ServiceStartupAssignsTerminalSvc' -v ./...
go test -race -count=1 -run 'TestTerminalService_ConcurrentAccess' -v ./...
    </automated>
    <automated>
# Integration tests (skip if no PTY or in CI):
go test -race -count=1 -run 'TestTerminalService_ProcessPersistAcrossSessionSwitch|TestTerminalService_OutputIsolation' -v ./...
    </automated>
  </verify>
  <done>All multi-session tests pass with race detector. Execution service test verifies default session creation. Existing single-session tests updated for new API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend→Backend (Wails binding) | sessionId and data passed from Terminal.tsx to Go methods |
| Go tests→TerminalService | Test code exercises public API (CreateSession, Write, CloseSession, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Denial of Service | Test race conditions | mitigate | All tests run with `-race` flag. `TestTerminalService_ConcurrentAccess` specifically exercises concurrent map access. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new npm packages installed. Wails bindings regeneration uses existing `wails3` CLI (already trusted). |
</threat_model>

<verification>
- `cd frontend && pnpm tsc --noEmit` passes
- `go test -race -count=1 -v ./...` passes
- `events.ts` has no PTY constants
- `Terminal.tsx` uses namespaced event names
- All 8 new multi-session tests pass
- All 10 existing tests updated and passing
</verification>

<success_criteria>
- [ ] `events.ts` PTY constants removed, `initEventNames` updated
- [ ] Wails bindings regenerated with `wails3 generate build-assets`
- [ ] `Terminal.tsx` accepts `sessionId` prop and constructs namespaced event names
- [ ] `Terminal.tsx` passes `sessionId` to Write, Resize, Clear, Start calls
- [ ] `Terminal.tsx` `useEffect` depends on `sessionId`
- [ ] Frontend TypeScript compiles clean: `pnpm tsc --noEmit`
- [ ] New test helpers: sessions map init in `newTestTerminalService`, `mustCreateAndStart`
- [ ] All existing 10 tests updated for multi-session API and passing
- [ ] New multi-session tests: CreateSession, ListSessions, RenameSession, CloseSession
- [ ] Integration tests: ProcessPersistAcrossSessionSwitch, OutputIsolation
- [ ] Safety tests: ShutdownCleansAll, ConcurrentAccess (under -race)
- [ ] `execution_service_test.go` updated: verifies default session after ServiceStartup
- [ ] `go test -race -count=1 -v ./...` green
</success_criteria>

<output>
Create `.planning/phases/21-backend-session-foundation/21-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

| Symbol | Kind | Location | Purpose |
|--------|------|----------|---------|
| `eventNames` (modified) | const object | frontend/src/wails/events.ts | ptyOutput, ptyExit, ptyCleared entries removed |
| `initEventNames()` (modified) | function | frontend/src/wails/events.ts | PTY assignments removed from binding sync |
| `TerminalComponentProps.sessionId` | prop (new) | frontend/src/components/Terminal.tsx | Session ID for namespaced event subscriptions and Go method dispatch |
| `TerminalComponent` (modified) | component | frontend/src/components/Terminal.tsx | Uses `"pty-output:"+sessionId` etc.; passes sessionId to Write/Resize/Clear/Start |
| `frontend/bindings/cmdex/terminalservice/` | generated files | directory | Regenerated TS bindings with sessionId parameter signatures |
| `frontend/bindings/cmdex/eventservice/` | generated files | directory | Regenerated TS bindings without PtyOutput/PtyExit/PtyCleared |
| `newTestTerminalService(t)` (modified) | test helper | terminal_service_test.go | Initializes sessions map on returned TerminalService |
| `mustCreateAndStart(t, s)` (new) | test helper | terminal_service_test.go | Creates session via CreateSession, registers cleanup, returns sessionId |
| `TestTerminalService_CreateSession` (new) | test | terminal_service_test.go | SESS-01: verifies UUID, name, running state, second session naming |
| `TestTerminalService_ListSessions` (new) | test | terminal_service_test.go | SESS-01: verifies list count after create and close |
| `TestTerminalService_RenameSession` (new) | test | terminal_service_test.go | SESS-04: verifies rename, empty rejection, nonexistent error |
| `TestTerminalService_CloseSession` (new) | test | terminal_service_test.go | SESS-05: verifies removal, active reassignment, double-close error |
| `TestTerminalService_ProcessPersistAcrossSessionSwitch` (new) | test | terminal_service_test.go | EXEC-04: verifies long-running process survives session switch |
| `TestTerminalService_OutputIsolation` (new) | test | terminal_service_test.go | EXEC-04: verifies session A output does not leak to session B |
| `TestTerminalService_NamespacedEvents` (new) | test | terminal_service_test.go | Verifies emitter uses `"pty-output:"+sessionId` format |
| `TestTerminalService_ShutdownCleansAll` (new) | test | terminal_service_test.go | Verifies ServiceShutdown empties sessions map |
| `TestTerminalService_ConcurrentAccess` (new) | test | terminal_service_test.go | Verifies concurrent map access does not panic under -race |
| `TestTerminalService_ServiceStartupAssignsTerminalSvc` (modified) | test | execution_service_test.go | Updated to verify default session creation after ServiceStartup |
