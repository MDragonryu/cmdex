---
phase: 26-dependency-bump
plan: 01
subsystem: infra
tags: [go, wails, deps]

requires: []
provides:
  - "Go toolchain bumped to 1.26.0"
  - "Wails v3 framework bumped from v3.0.0-alpha.74 to v3.0.0-alpha2.104 (~820 commits)"
  - "wails3 CLI installed at v3.0.0-alpha2.104"
  - "frontend/bindings/ regenerated (functionally identical, zero API breakage)"
  - "Makefile check target fixed to exclude ios/ package generated by new wails3 CLI"
affects: []

tech-stack:
  added: []
  patterns: []

key-files:
  modified:
    - "go.mod"
    - "go.sum"
    - "Makefile"
    - "frontend/bindings/ (regenerated, no semantic changes)"

key-decisions:
  - "Wails v3.0.0-alpha.74 → v3.0.0-alpha2.104: zero API breakage confirmed — all 20+ consumed API surfaces identical"
  - "New wails3 CLI generates ios/ scaffold that breaks go build ./...; fixed Makefile to exclude it"

patterns-established: []

requirements-completed: []

duration: 5 min
completed: 2026-06-19
---

# Phase 26 Plan 01: Dependency Version Bump Summary

**Bumped Go toolchain to 1.26.0 and Wails v3 framework to v3.0.0-alpha2.104 (~820 commits) with zero API breakage. All automated checks pass (build, vet, test, TypeScript).**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-19T03:53:00Z
- **Completed:** 2026-06-19T03:58:00Z
- **Tasks:** 2
- **Files modified:** 3 (go.mod, go.sum, Makefile)

## Accomplishments

- Go toolchain directive: `go 1.25.0` → `go 1.26.0`
- Wails framework: `v3.0.0-alpha.74` → `v3.0.0-alpha2.104`
- wails3 CLI installed at matching version (`go install @v3.0.0-alpha2.104`)
- frontend/bindings/ regenerated — functionally identical (zero API breakage confirmed by RESEARCH.md)
- All 45 tests pass, Go build + vet pass, TypeScript compilation passes

## Task Commits

Each task was committed atomically:

1. **Task 1: Bump go.mod versions, tidy, and verify Go build** - `ca7843c` (chore)
2. **Task 2: Install matching wails3 CLI, regenerate bindings, run full checks** - `6298d7f` (chore)

## Files Created/Modified

- `go.mod` - Go toolchain directive (line 3: `go 1.26.0`) and Wails require (line 9: `v3.0.0-alpha2.104`)
- `go.sum` - Updated checksums for Wails v3.0.0-alpha2.104 and transitive dependencies (e.g., `wailsapp/wails/webview2 v1.0.24`)
- `Makefile` - Excluded `ios/` package from `go build` in check target (new wails3 CLI generates ios scaffold)
- `frontend/bindings/` - Regenerated via `wails3 generate build-assets` (functionally identical, zero API breakage)

## Decisions Made

- Made `go build ./...` skip the `ios/` package via Makefile grep filter — the new wails3 CLI scaffolds an `ios/` directory with build-tagged `main` package that can't compile without `ios` tag
- Confirmed all 20+ consumed Wails v3 APIs are identical between alpha.74 and alpha2.104 (per RESEARCH.md source comparison)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] ios/ package breaks `go build ./...` after wails3 generate build-assets**

- **Found during:** Task 2 (regenerate bindings → make check)
- **Issue:** The new wails3 CLI (v3.0.0-alpha2.104) generates an `ios/` directory with `//go:build ios`-tagged Go files. The `app_options_default.go` file (`//go:build !ios`) declares `package main` without a `main` function, causing `go build ./...` to fail on non-ios platforms.
- **Fix:** Modified Makefile check target to exclude `ios/` package via `go list ./... | grep -v '/ios$'`
- **Files modified:** Makefile
- **Verification:** `make check` exits 0 (Go build + pnpm tsc --noEmit pass)
- **Committed in:** `6298d7f` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 missing critical)
**Impact on plan:** Minor Makefile adjustment to accommodate new wails3 scaffold output. No scope creep.

## Issues Encountered

- Linux cross-compile (`GOOS=linux`) fails with GTK/CGo undefined symbol errors — pre-existing, not introduced by this bump. Windows cross-compile passes cleanly.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 26 complete. All verification gates pass (build, vet, test, TypeScript).
- Ready for verification step.

---
*Phase: 26-dependency-bump*
*Completed: 2026-06-19*
