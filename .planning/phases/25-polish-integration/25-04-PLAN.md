---
phase: 25-polish-integration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ROADMAP.md
  - REQUIREMENTS.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "ROADMAP.md Phase 25 success criteria list reflects the in-memory set (memory leaks, conpty, cwd inheritance, dead-code cleanup, error states) and no longer claims session persistence across restarts (D-02)"
    - "REQUIREMENTS.md moves PERS-01, PERS-02, PERS-03, PERS-04 from the v1 Session Persistence section to the v2 Enhanced Persistence section (D-03)"
    - "REQUIREMENTS.md traceability table maps PERS-01..PERS-04 to v2 (deferred), not Phase 22 (D-03)"
    - "REQUIREMENTS.md v1 requirement count drops from 22 to 18 mapped, and v2 total grows from 6 to 10 (D-03)"
  artifacts:
    - path: "ROADMAP.md"
      provides: "Phase 25 success criteria updated to in-memory set (5 items)"
      contains: "No memory leaks"
    - path: "REQUIREMENTS.md"
      provides: "PERS-01..PERS-04 moved from v1 to v2 section"
      contains: "### Session Persistence (deferred from v1)"
  key_links:
    - from: "ROADMAP.md"
      to: "Phase 25 entry"
      via: "section header"
      pattern: "Phase 25: Polish & Integration"
    - from: "REQUIREMENTS.md traceability table"
      to: "PERS-* rows"
      via: "Phase column"
      pattern: "v2 \\(deferred\\)"
---

<objective>
Update ROADMAP.md and REQUIREMENTS.md to reflect the in-memory reality of v2.1. Per D-02, replace Phase 25 success criteria in ROADMAP.md with the in-memory set (5 items: memory leak, conpty, cwd inheritance, dead-code cleanup, error states). Per D-03, move PERS-01..PERS-04 from v1 to v2 Requirements in REQUIREMENTS.md and update the traceability table. No code changes — this is a documentation-only plan and runs in parallel with the code-bearing plans.

Citations: D-02 (ROADMAP.md success criteria replacement), D-03 (PERS-01..PERS-04 moved to v2).
</objective>

<execution_context>
@/Users/mac/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/mac/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-polish-integration/25-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 25 success criteria + move PERS-01..PERS-04 to v2 in REQUIREMENTS.md</name>
  <files>ROADMAP.md, REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 25 entry (lines 119-135) — the success criteria list (lines 127-134) to be replaced
    - .planning/REQUIREMENTS.md v1 Session Persistence section (lines 37-42) — the four PERS-01..PERS-04 entries to be moved
    - .planning/REQUIREMENTS.md v2 Enhanced Persistence section (lines 60-62) — currently contains only PERS-05 and PERS-06; the four moved entries are appended here
    - .planning/REQUIREMENTS.md Traceability table (lines 76-101) — rows for PERS-01..PERS-04 to be updated to map to v2
    - .planning/REQUIREMENTS.md Coverage line (around line 105) — the totals to be updated
  </read_first>
  <action>
    Step 1: Update `ROADMAP.md` Phase 25 entry. Locate the line `### Phase 25: Polish & Integration` (line 119). Below the `**Goal:**` line and the `**Depends on:**` line, replace the `**Success Criteria** (what must be TRUE):` block (currently 5 items at lines 127-134) with a new 5-item list that matches the in-memory set from CONTEXT.md. The new items, in this order:
    1. `No memory leaks: rapid create/close cycles (100x) leave goroutine count stable, sessions map empty, and PTY FDs closed`
    2. `Windows conpty compatibility: code cross-compiles to Windows and the conpty stub is in place; full runtime verification deferred`
    3. `Global default cwd inheritance: new sessions inherit the global default working directory at creation; existing sessions unchanged`
    4. `Dead-code cleanup: no remnants of OutputPane, cmd-output event, or RunInTerminal in the source`
    5. `Error states: max sessions limit enforced, last-session close guarded, PTY start failure surfaced as a toast`

    Also update the `**Plans:**` line of the Phase 25 entry. The current text is `**Plans:** TBD` (line 135). Change it to `**Plans:** 4 plans` to reflect the four PLAN.md files this planning effort produces. The `**UI hint**: no` line is preserved as-is.

    Step 2: Update `REQUIREMENTS.md` v1 Session Persistence section. The section currently reads (lines 37-42):
    ```
    ### Session Persistence

    - [ ] **PERS-01**: Terminal sessions persist across app restarts (name, working dir, shell)
    - [ ] **PERS-02**: On startup, previous sessions are restored and available in tab bar
    - [ ] **PERS-03**: Session working directory is restored (per-command → global default → OS home fallback)
    - [ ] **PERS-04**: Active session is remembered and auto-selected on restart
    ```
    Delete these four lines entirely from the v1 section. After deletion, the v1 section will have no entries under `### Session Persistence`. Either remove the `### Session Persistence` heading and its blank-line neighbors, or leave the heading and let the section be empty. The cleanest result is to delete the heading and the four bullets together, leaving the section omitted.

    Step 3: Update `REQUIREMENTS.md` v2 Enhanced Persistence section. The section currently contains only PERS-05 and PERS-06 (lines 60-62). Insert a new subsection above the existing two entries (or restructure the section to first list the deferred-from-v1 items, then the originally-deferred items). The new subsection heading is `### Session Persistence (deferred from v1)` and contains the four PERS-01..PERS-04 entries. Use unchecked checkboxes (`- [ ]`) to match the style of PERS-05 and PERS-06 below.

    The final v2 section should look like:
    ```
    ### Enhanced Persistence

    #### Session Persistence (deferred from v1)

    - [ ] **PERS-01**: Terminal sessions persist across app restarts (name, working dir, shell)
    - [ ] **PERS-02**: On startup, previous sessions are restored and available in tab bar
    - [ ] **PERS-03**: Session working directory is restored (per-command → global default → OS home fallback)
    - [ ] **PERS-04**: Active session is remembered and auto-selected on restart

    - **PERS-05**: Session scrollback history persisted across restarts
    - **PERS-06**: Shell command history persisted per session
    ```
    Adjust the heading levels to match the existing style (the file uses `###` for top-level v2 subsections; `####` is acceptable for a sub-grouping).

    Step 4: Update `REQUIREMENTS.md` Traceability table (lines 76-101). Locate the four rows for PERS-01..PERS-04 (lines 98-101). Change the `Phase` column from `Phase 22` to `v2 (deferred)`. The `Status` column for all four rows is `Pending`; leave that unchanged. The four updated rows should read:
    ```
    | PERS-01 | v2 (deferred) | Pending |
    | PERS-02 | v2 (deferred) | Pending |
    | PERS-03 | v2 (deferred) | Pending |
    | PERS-04 | v2 (deferred) | Pending |
    ```

    Step 5: Update the `**Coverage:**` line at the end of REQUIREMENTS.md (around line 105-106). The current text reads:
    ```
    **Coverage:**
    - v1 requirements: 22 total
    - Mapped to phases: 22
    - Unmapped: 0 ✓
    ```
    Update to:
    ```
    **Coverage:**
    - v1 requirements: 18 total
    - Mapped to phases: 18
    - Unmapped: 0 ✓
    - v2 requirements: 10 total (4 deferred from v1, 6 originally deferred)
    ```
    The math: 22 v1 minus 4 PERS moved out = 18 v1. v2 was 6, plus 4 newly moved = 10 v2.

    Step 6: Verify the two files render correctly. Open both files and visually confirm:
    - ROADMAP.md Phase 25 success criteria has 5 items, none of which claim persistence across app restarts.
    - REQUIREMENTS.md v1 has no PERS-01..PERS-04 entries. REQUIREMENTS.md v2 has all four PERS-01..PERS-04 entries plus the original PERS-05 and PERS-06. The traceability table has the four PERS-01..PERS-04 rows with `v2 (deferred)`. The Coverage line shows 18 v1 mapped and 10 v2 total.
  </action>
  <acceptance_criteria>
    - `ROADMAP.md` Phase 25 `**Success Criteria**` block contains the string `No memory leaks` (verified by grep)
    - `ROADMAP.md` Phase 25 `**Success Criteria**` block contains the string `Windows conpty compatibility` (verified by grep)
    - `ROADMAP.md` Phase 25 `**Success Criteria**` block contains the string `Global default cwd inheritance` (verified by grep)
    - `ROADMAP.md` Phase 25 `**Success Criteria**` block contains the string `Dead-code cleanup` (verified by grep)
    - `ROADMAP.md` Phase 25 `**Success Criteria**` block contains the string `Error states` (verified by grep)
    - `ROADMAP.md` Phase 25 `**Success Criteria**` block no longer contains the string `persist across app restarts` (verified by `! grep`)
    - `ROADMAP.md` Phase 25 `**Plans:**` line reads `4 plans` (verified by grep)
    - `REQUIREMENTS.md` v1 section (under `## v1 Requirements`) does not contain `PERS-01`, `PERS-02`, `PERS-03`, or `PERS-04` (verified by `grep -c` on the v1 lines returning 0)
    - `REQUIREMENTS.md` v2 section (under `## v2 Requirements`) contains all four PERS-01..PERS-04 entries (verified by `grep -c "PERS-0[1-4]"` returning >= 4 in the v2 region)
    - `REQUIREMENTS.md` traceability table contains at least one row mapping a PERS-NN requirement to `v2 (deferred)` (verified by grep)
    - `REQUIREMENTS.md` `**Coverage:**` block reports `18 total` for v1 (verified by grep)
    - `REQUIREMENTS.md` `**Coverage:**` block reports `10 total` for v2 (verified by grep)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "No memory leaks" .planning/ROADMAP.md && grep -c "Windows conpty compatibility" .planning/ROADMAP.md && grep -c "Global default cwd inheritance" .planning/ROADMAP.md && (test "$(grep -c PERS-01 .planning/REQUIREMENTS.md)" -ge 4 || (grep -A 30 '## v2 Requirements' .planning/REQUIREMENTS.md | grep -c PERS-01) ) && echo "all checks passed"</automated>
  </verify>
  <done>
    ROADMAP.md Phase 25 success criteria reflect the in-memory set (5 items, no persistence claims). REQUIREMENTS.md v1 has no PERS-01..PERS-04 entries; REQUIREMENTS.md v2 has all four plus the original PERS-05 and PERS-06; the traceability table maps PERS-01..PERS-04 to v2 (deferred); the Coverage line shows 18 v1 mapped and 10 v2 total. The documentation is internally consistent: a reader of the ROADMAP will not see claims that the phase does not deliver, and a reader of REQUIREMENTS.md will see PERS-01..PERS-04 correctly placed in the v2 (deferred) bucket.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan edits two planning documents (ROADMAP.md, REQUIREMENTS.md). No code, no interfaces, no trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Repudiation | Documentation drift | mitigate | Both edits are pinned to specific locked decisions (D-02 and D-03) and verified by grep. The Phase 25 SUMMARY will record the doc edits with the exact text changes. The `gsd-health` workflow can later detect drift between ROADMAP claims and REQUIREMENTS traceability. |
| T-25-04-SC | Tampering | Markdown files | accept | These are documentation files, not code. No executable surface. The CI does not run them. |
</threat_model>

<verification>
- ROADMAP.md Phase 25 success criteria list contains the 5 in-memory items and no persistence claim
- REQUIREMENTS.md v1 has no PERS-01..PERS-04 entries
- REQUIREMENTS.md v2 has all four PERS-01..PERS-04 entries
- REQUIREMENTS.md traceability table maps PERS-01..PERS-04 to v2 (deferred)
- REQUIREMENTS.md Coverage line shows 18 v1 mapped and 10 v2 total
- No code files were modified by this plan (verified by `git status` showing only the two `.md` files changed)
</verification>

<success_criteria>
- ROADMAP.md Phase 25 success criteria are replaced with the in-memory set (D-02)
- REQUIREMENTS.md PERS-01..PERS-04 are moved from v1 to v2 (D-03)
- REQUIREMENTS.md traceability table reflects the move (D-03)
- REQUIREMENTS.md coverage counts are updated
- No code changes are introduced by this plan
</success_criteria>

<output>
Create `.planning/phases/25-polish-integration/25-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

**Modified doc (`.planning/ROADMAP.md`):**
- Phase 25 success criteria list replaced with the in-memory set (5 items: memory leaks, conpty, cwd inheritance, dead-code cleanup, error states)
- Phase 25 `**Plans:**` line updated from `TBD` to `4 plans`

**Modified doc (`.planning/REQUIREMENTS.md`):**
- v1 Session Persistence section: the four PERS-01..PERS-04 entries removed
- v2 Enhanced Persistence section: a new `#### Session Persistence (deferred from v1)` sub-heading added containing the four PERS-01..PERS-04 entries (above the existing PERS-05 and PERS-06)
- Traceability table: the four PERS-01..PERS-04 rows have their `Phase` column changed from `Phase 22` to `v2 (deferred)`
- `**Coverage:**` block: v1 total changed from `22 total` to `18 total`; a new `v2 requirements: 10 total` line added
